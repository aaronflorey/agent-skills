# Shieldsio - Badges

**Pages:** 601

---

## Read the Docs (version) | Shields.io

**URL:** https://shields.io/badges/read-the-docs-version

**Contents:**
- Read the Docs (version)

ReadTheDocs is a hosting service for documentation.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ubuntu Package Version | Shields.io

**URL:** https://shields.io/badges/ubuntu-package-version

**Contents:**
- Ubuntu Package Version

Ubuntu Package Version

Example: ubuntu-wallpapers

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitea Last Commit | Shields.io

**URL:** https://shields.io/badges/gitea-last-commit

**Contents:**
- Gitea Last Commit

By default this badge looks for repositories on gitea.com. To specify another instance like codeberg, forgejo or a self-hosted instance, use the gitea_url query param.

File path to resolve the last commit for.

Possible values: [author, committer]

Defaults to author if not specified

Example: https://gitea.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Depfu | Shields.io

**URL:** https://shields.io/badges/depfu

**Contents:**
- Depfu

Possible values: [github, gitlab]

Example: depfu/example-ruby

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Plugin Required PHP Version | Shields.io

**URL:** https://shields.io/badges/word-press-plugin-required-php-version

**Contents:**
- WordPress Plugin Required PHP Version

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Relative date | Shields.io

**URL:** https://shields.io/badges/relative-date

**Contents:**
- Relative date

Supply a unix timestamp in seconds to display the relative time from/to now

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Issues or Pull Requests by label | Shields.io

**URL:** https://shields.io/badges/git-hub-issues-or-pull-requests-by-label

**Contents:**
- GitHub Issues or Pull Requests by label

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [issues, issues-raw, issues-closed, issues-closed-raw, issues-pr, issues-pr-raw, issues-pr-closed, issues-pr-closed-raw]

Example: service-badge

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub repo file or directory count | Shields.io

**URL:** https://shields.io/badges/git-hub-repo-file-or-directory-count

**Contents:**
- GitHub repo file or directory count

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [dir, file]

Entity to count: directories or files. If not specified, both files and directories are counted. GitHub API has an upper limit of 1,000 files for a directory. If a directory contains files above the limit, the badge will show an inaccurate count.

Filter to files of type. Specify the extension without a leading dot. For instance for .js extension pass js. This param is only applicable if type is file

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub package.json version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-package-json-version-branch

**Contents:**
- GitHub package.json version (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: badge-maker/package.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer coverage (GitLab) | Shields.io

**URL:** https://shields.io/badges/scrutinizer-coverage-git-lab

**Contents:**
- Scrutinizer coverage (GitLab)

Scrutinizer coverage (GitLab)

Example: propertywindow

Example: propertywindow

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Testspace tests | Shields.io

**URL:** https://shields.io/badges/testspace-tests

**Contents:**
- Testspace tests

You may change the "passed", "failed" and "skipped" text on this badge by supplying query parameters &passed_label=, &failed_label= and &skipped_label= respectively.

For example, if you want to use a different terminology:

?passed_label=good&failed_label=bad&skipped_label=n%2Fa

?compact_message&passed_label=💃&failed_label=🤦‍♀️&skipped_label=🤷

There is also a &compact_message query parameter, which will default to displaying ✔, ✘ and ➟, separated by a horizontal bar |.

Example: swellaby:testspace-sample

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Galaxy Toolshed - Repository Version | Shields.io

**URL:** https://shields.io/badges/galaxy-toolshed-repository-version

**Contents:**
- Galaxy Toolshed - Repository Version

Galaxy Toolshed - Repository Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AUR Last Modified | Shields.io

**URL:** https://shields.io/badges/aur-last-modified

**Contents:**
- AUR Last Modified

Arch linux User Repository Last Modified

Example: google-chrome

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Release Date | Shields.io

**URL:** https://shields.io/badges/git-hub-release-date

**Contents:**
- GitHub Release Date

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [release-date, release-date-pre]

Example: release-date

Example: SubtitleEdit

Example: subtitleedit

Possible values: [created_at, published_at]

Default value is created_at if not specified

Example: published_at

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM Version (with dist tag) | Shields.io

**URL:** https://shields.io/badges/npm-version-with-dist-tag

**Contents:**
- NPM Version (with dist tag)

NPM Version (with dist tag)

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## OSSF-Scorecard Score | Shields.io

**URL:** https://shields.io/badges/ossf-scorecard-score

**Contents:**
- OSSF-Scorecard Score

Example: org-workflow-add

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PyPI - License | Shields.io

**URL:** https://shields.io/badges/py-pi-license

**Contents:**
- PyPI - License

Example: https://pypi.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Cocoapods Version | Shields.io

**URL:** https://shields.io/badges/cocoapods-version

**Contents:**
- Cocoapods Version

Example: AFNetworking

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Treeware (Trees) | Shields.io

**URL:** https://shields.io/badges/treeware-trees

**Contents:**
- Treeware (Trees)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Spiget Downloads | Shields.io

**URL:** https://shields.io/badges/spiget-downloads

**Contents:**
- Spiget Downloads

Spiget holds information about SpigotMC Resources, Plugins and Authors.

You can find your resource ID in the url for your resource page.

Example: https://www.spigotmc.org/resources/essentialsx.9089/ - Here the Resource ID is 9089.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SymfonyInsight Violations | Shields.io

**URL:** https://shields.io/badges/symfony-insight-violations

**Contents:**
- SymfonyInsight Violations

SymfonyInsight (formerly SensioLabs) is a code analysis service

Example: 825be328-29f8-44f7-a750-f82818ae9111

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Pipenv locked dependency version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-pipenv-locked-dependency-version-branch

**Contents:**
- GitHub Pipenv locked dependency version (branch)

Pipenv is a dependency manager for Python which manages a virtualenv for projects. It adds/removes packages from your Pipfile as you install/uninstall packages and generates the ever-important Pipfile.lock, which can be checked in to source control in order to produce deterministic builds.

The GitHub Pipenv badges are intended for applications using Pipenv which are hosted on GitHub.

When Pipfile.lock is checked in, the GitHub Pipenv locked dependency version badge displays the locked version of a dependency listed in [packages] or [dev-packages] (or any of their transitive dependencies).

Usually a Python version is specified in the Pipfile, which pipenv lock then places in Pipfile.lock. The GitHub Pipenv Python version badge displays that version.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: rq-dashboard-on-heroku

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gnome Extensions Downloads | Shields.io

**URL:** https://shields.io/badges/gnome-extensions-downloads

**Contents:**
- Gnome Extensions Downloads

Gnome Extensions Downloads

Id of the Gnome Extension

Example: just-perfection-desktop@just-perfection

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Documented API Density | Shields.io

**URL:** https://shields.io/badges/sonar-documented-api-density

**Contents:**
- Sonar Documented API Density

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: brave_brave-core

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Hacktoberfest combined status | Shields.io

**URL:** https://shields.io/badges/git-hub-hacktoberfest-combined-status

**Contents:**
- GitHub Hacktoberfest combined status

This badge is designed for projects hosted on GitHub which are participating in Hacktoberfest, an initiative to encourage participating in open-source projects. The badge can be added to the project readme to encourage potential contributors to review the suggested issues and to celebrate the contributions that have already been made. The badge displays three pieces of information:

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [2019, 2020, 2021, 2022, 2023, 2024, 2025]

Example: tmrowapp-contrib

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PyPI - Implementation | Shields.io

**URL:** https://shields.io/badges/py-pi-implementation

**Contents:**
- PyPI - Implementation

PyPI - Implementation

Example: https://pypi.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Docker Automated build | Shields.io

**URL:** https://shields.io/badges/docker-automated-build

**Contents:**
- Docker Automated build

Docker Automated build

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Mozilla Add-on Users | Shields.io

**URL:** https://shields.io/badges/mozilla-add-on-users

**Contents:**
- Mozilla Add-on Users

addons.mozilla.org (AMO) publishes extensions for Mozilla Firefox

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Jenkins Build | Shields.io

**URL:** https://shields.io/badges/jenkins-build

**Contents:**
- Jenkins Build

Example: https://ci.eclipse.org/jgit/job/jgit

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Puppet Forge releases by user | Shields.io

**URL:** https://shields.io/badges/puppet-forge-releases-by-user

**Contents:**
- Puppet Forge releases by user

Puppet Forge releases by user

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Chrome Web Store Users | Shields.io

**URL:** https://shields.io/badges/chrome-web-store-users

**Contents:**
- Chrome Web Store Users

Our servers make requests to the Chrome Web Store from the US East region. Therefore, the extension must not be regionally restricted by the publisher for that location.

Example: ogffaloegjglncjfehdfplabnoondfjo

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io Size (version) | Shields.io

**URL:** https://shields.io/badges/crates-io-size-version

**Contents:**
- Crates.io Size (version)

Crates.io is a package registry for Rust.

Example: rustc-serialize

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub commits since latest release (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-commits-since-latest-release-branch

**Contents:**
- GitHub commits since latest release (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

The include_prereleases, sort and filter params can be used to configure how we determine the latest version.

Example: SubtitleEdit

Example: subtitleedit

Possible values: [date, semver]

The filter param can be used to apply a filter to the project's tag or release names before selecting the latest from the list. Two constructs are available: * is a wildcard matching zero or more characters, and if the pattern starts with a !, the whole pattern is negated.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Docker Image Size (tag) | Shields.io

**URL:** https://shields.io/badges/docker-image-size-tag

**Contents:**
- Docker Image Size (tag)

Docker Image Size (tag)

Possible values: [date, semver]

If not specified, the default is date

Possible values: [amd64, arm, arm64, s390x, 386, ppc64, ppc64le, wasm, mips, mipsle, mips64, mips64le, riscv64, loong64]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AppVeyor Job | Shields.io

**URL:** https://shields.io/badges/app-veyor-job

**Contents:**
- AppVeyor Job

Example: wpmgprostotema

Example: voicetranscoder

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Eclipse Marketplace License | Shields.io

**URL:** https://shields.io/badges/eclipse-marketplace-license

**Contents:**
- Eclipse Marketplace License

Eclipse Marketplace License

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Test Execution Time | Shields.io

**URL:** https://shields.io/badges/sonar-test-execution-time

**Contents:**
- Sonar Test Execution Time

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Tech Debt | Shields.io

**URL:** https://shields.io/badges/sonar-tech-debt

**Contents:**
- Sonar Tech Debt

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: brave_brave-core

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## UptimeObserver uptime ratio (30 days) | Shields.io

**URL:** https://shields.io/badges/uptime-observer-uptime-ratio-30-days

**Contents:**
- UptimeObserver uptime ratio (30 days)

UptimeObserver uptime ratio (30 days)

Example: 33Zw1rnH6veb4OLcskqvj6g9Lj4tnyxZ41

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Chrome Web Store Version | Shields.io

**URL:** https://shields.io/badges/chrome-web-store-version

**Contents:**
- Chrome Web Store Version

Our servers make requests to the Chrome Web Store from the US East region. Therefore, the extension must not be regionally restricted by the publisher for that location.

Example: ogffaloegjglncjfehdfplabnoondfjo

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## DUB License | Shields.io

**URL:** https://shields.io/badges/dub-license

**Contents:**
- DUB License

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub package.json dynamic | Shields.io

**URL:** https://shields.io/badges/git-hub-package-json-dynamic

**Contents:**
- GitHub package.json dynamic

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

any key in package.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Codacy grade | Shields.io

**URL:** https://shields.io/badges/codacy-grade

**Contents:**
- Codacy grade

Example: 0cb32ce695b743d68257021455330c66

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scoop Version | Shields.io

**URL:** https://shields.io/badges/scoop-version

**Contents:**
- Scoop Version

Scoop is a command-line installer for Windows

App's containing bucket. Can either be a name (e.g extras) or a URL to a GitHub Repo (e.g https://github.com/jewlexx/personal-scoop)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer quality (GitHub/Bitbucket) with branch | Shields.io

**URL:** https://shields.io/badges/scrutinizer-quality-git-hub-bitbucket-with-branch

**Contents:**
- Scrutinizer quality (GitHub/Bitbucket) with branch

Scrutinizer quality (GitHub/Bitbucket) with branch

Possible values: [g, b]

Platform: Either GitHub or Bitbucket

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Package Control Downloads | Shields.io

**URL:** https://shields.io/badges/package-control-downloads

**Contents:**
- Package Control Downloads

Package Control is a package registry for Sublime Text packages

Possible values: [dd, dw, dm, dt]

Daily, Weekly, Monthly, or Total downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Dynamic XML Badge | Shields.io

**URL:** https://shields.io/badges/dynamic-xml-badge

**Contents:**
- Dynamic XML Badge

The Dynamic XML Badge allows you to extract an arbitrary value from any XML Document using an XPath selector and show it on a badge.

Useful resources for constructing XPath selectors:

Note: For XML documents that use a default namespace prefix, you will need to use the local-name function to construct your query. For example /*[local-name()='myelement'] rather than /myelement.

The URL to a XML document

Example: https://httpbin.org/xml

An XPath expression that will be used to query the document

Example: //slideshow/slide[1]/title

Optional prefix to append to the value

Optional suffix to append to the value

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Conda Platform | Shields.io

**URL:** https://shields.io/badges/conda-platform

**Contents:**
- Conda Platform

Possible values: [p, pn]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM Unpacked Size (with version) | Shields.io

**URL:** https://shields.io/badges/npm-unpacked-size-with-version

**Contents:**
- NPM Unpacked Size (with version)

NPM Unpacked Size (with version)

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub commits since tagged version | Shields.io

**URL:** https://shields.io/badges/git-hub-commits-since-tagged-version

**Contents:**
- GitHub commits since tagged version

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: SubtitleEdit

Example: subtitleedit

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Pub Monthly Downloads | Shields.io

**URL:** https://shields.io/badges/pub-monthly-downloads

**Contents:**
- Pub Monthly Downloads

Pub is a package registry for Dart and Flutter.

This badge shows a measure of how many developers have downloaded a package monthly.

Example: analysis_options

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Chrome Web Store Stars | Shields.io

**URL:** https://shields.io/badges/chrome-web-store-stars

**Contents:**
- Chrome Web Store Stars

Our servers make requests to the Chrome Web Store from the US East region. Therefore, the extension must not be regionally restricted by the publisher for that location.

Example: ogffaloegjglncjfehdfplabnoondfjo

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Subreddit subscribers | Shields.io

**URL:** https://shields.io/badges/subreddit-subscribers

**Contents:**
- Subreddit subscribers

Subreddit subscribers

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Homebrew Formula Downloads | Shields.io

**URL:** https://shields.io/badges/homebrew-formula-downloads

**Contents:**
- Homebrew Formula Downloads

Homebrew Formula Downloads

Possible values: [dm, dq, dy]

Monthly, Quarterly or Yearly downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Test Success Rate | Shields.io

**URL:** https://shields.io/badges/sonar-test-success-rate

**Contents:**
- Sonar Test Success Rate

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Spiget Version | Shields.io

**URL:** https://shields.io/badges/spiget-version

**Contents:**
- Spiget Version

Spiget holds information about SpigotMC Resources, Plugins and Authors.

You can find your resource ID in the url for your resource page.

Example: https://www.spigotmc.org/resources/essentialsx.9089/ - Here the Resource ID is 9089.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CTAN License | Shields.io

**URL:** https://shields.io/badges/ctan-license

**Contents:**
- CTAN License

CTAN is a package registry for TeX.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonatype Nexus (Repository) | Shields.io

**URL:** https://shields.io/badges/sonatype-nexus-repository

**Contents:**
- Sonatype Nexus (Repository)

Sonatype Nexus (Repository)

Example: maven-central

Example: com.google.guava

Example: https://repo.tomkeuper.com

Note that you can use query options with any Nexus badge type (Releases, Snapshots, or Repository).

Query options should be provided as key=value pairs separated by a colon.

Possible values: Searching for Component

Example: :c=agent-apple-osx:p=tar.gz

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## OSS Lifecycle | Shields.io

**URL:** https://shields.io/badges/oss-lifecycle

**Contents:**
- OSS Lifecycle

OSS Lifecycle is an initiative started by Netflix to classify open-source projects into lifecycles and clearly identify which projects are active and which ones are retired. To enable this badge, simply create an OSSMETADATA tagging file at the root of your repository containing a single line similar to the following: osslifecycle=active. Other suggested values are osslifecycle=maintenance and osslifecycle=archived. A working example can be viewed on the OSS Tracker repository.

URL for the OSSMETADATA file

Example: https://raw.githubusercontent.com/Netflix/aws-autoscaling/master/OSSMETADATA

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Drone (branch) | Shields.io

**URL:** https://shields.io/badges/drone-branch

**Contents:**
- Drone (branch)

Example: https://drone.shields.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Dynamic JSON Badge | Shields.io

**URL:** https://shields.io/badges/dynamic-json-badge

**Contents:**
- Dynamic JSON Badge

The Dynamic JSON Badge allows you to extract an arbitrary value from any JSON Document using a JSONPath selector and show it on a badge.

The URL to a JSON document

Example: https://github.com/badges/shields/raw/master/package.json

A JSONPath expression that will be used to query the document

Optional prefix to append to the value

Optional suffix to append to the value

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Plugin Version | Shields.io

**URL:** https://shields.io/badges/word-press-plugin-version

**Contents:**
- WordPress Plugin Version

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Deps.rs Crate Dependencies (specific version) | Shields.io

**URL:** https://shields.io/badges/deps-rs-crate-dependencies-specific-version

**Contents:**
- Deps.rs Crate Dependencies (specific version)

Deps.rs is a service that checks dependency security status in Rust projects.

The name of the Rust crate

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PiWheels Version | Shields.io

**URL:** https://shields.io/badges/pi-wheels-version

**Contents:**
- PiWheels Version

PiWheels is a Python package repository providing Arm platform wheels for the Raspberry Pi

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## YouTube Video Comments | Shields.io

**URL:** https://shields.io/badges/you-tube-video-comments

**Contents:**
- YouTube Video Comments

Shields.io is committed to protecting the privacy of its users. We do not collect, store, or track any personal information or data returned by the YouTube API Services. Our service is designed to generate badges based on public YouTube data, and we do not retain any user-specific information:

By using the YouTube badge, you are:

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NuGet Version | Shields.io

**URL:** https://shields.io/badges/nu-get-version

**Contents:**
- NuGet Version

Possible values: [v, vpre]

Latest stable version (v) or Latest version including prereleases (vpre).

Example: Microsoft.AspNet.Mvc

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Maven metadata URL | Shields.io

**URL:** https://shields.io/badges/maven-metadata-url

**Contents:**
- Maven metadata URL

Example: https://repo1.maven.org/maven2/com/google/guava/guava/maven-metadata.xml

Possible values: [highestVersion, releaseProperty, latestProperty]

The strategy used to determine the version that will be shown

Example: highestVersion

The filter param can be used to apply a filter to the project's versions before selecting the latest from the list. Two constructs are available: * is a wildcard matching zero or more characters, and if the pattern starts with a !, the whole pattern is negated.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Galaxy Toolshed - Downloads | Shields.io

**URL:** https://shields.io/badges/galaxy-toolshed-downloads

**Contents:**
- Galaxy Toolshed - Downloads

Galaxy Toolshed - Downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io Total Downloads | Shields.io

**URL:** https://shields.io/badges/crates-io-total-downloads

**Contents:**
- Crates.io Total Downloads

Crates.io is a package registry for Rust.

Example: rustc-serialize

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Documented API Density (branch) | Shields.io

**URL:** https://shields.io/badges/sonar-documented-api-density-branch

**Contents:**
- Sonar Documented API Density (branch)

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitlab Pipeline Status | Shields.io

**URL:** https://shields.io/badges/gitlab-pipeline-status

**Contents:**
- Gitlab Pipeline Status

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Important: You must use the Project Path, not the Project Id. Additionally, if your project is publicly visible, but the badge is like this:

Check if your pipelines are publicly visible as well. Navigate to your project settings on GitLab and choose General Pipelines under CI/CD. Then tick the setting Public pipelines.

Now your settings should look like this:

Your badge should be working fine now.

NB - The badge will display 'inaccessible' if the specified repo was not found on the target Gitlab instance.

Example: gitlab-org/gitlab

Example: https://gitlab.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Open Collective backers and sponsors | Shields.io

**URL:** https://shields.io/badges/open-collective-backers-and-sponsors

**Contents:**
- Open Collective backers and sponsors

Open Collective backers and sponsors

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Weblate entities | Shields.io

**URL:** https://shields.io/badges/weblate-entities

**Contents:**
- Weblate entities

Weblate is an web-based tool for translation and internationalization

Possible values: [components, projects, users, languages]

Example: https://hosted.weblate.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Mbin | Shields.io

**URL:** https://shields.io/badges/mbin

**Contents:**
- Mbin

Mbin is a fork of Kbin, a content aggregator for the Fediverse.

The magazine to query. This is CASE SENSITIVE. Use URL encoding for special characters.

Example: kbinEarth@kbin.earth

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Jenkins Tests | Shields.io

**URL:** https://shields.io/badges/jenkins-tests

**Contents:**
- Jenkins Tests

You may change the "passed", "failed" and "skipped" text on this badge by supplying query parameters &passed_label=, &failed_label= and &skipped_label= respectively.

For example, if you want to use a different terminology:

?passed_label=good&failed_label=bad&skipped_label=n%2Fa

?compact_message&passed_label=💃&failed_label=🤦‍♀️&skipped_label=🤷

There is also a &compact_message query parameter, which will default to displaying ✔, ✘ and ➟, separated by a horizontal bar |.

Example: https://ci.eclipse.org/jgit/job/jgit

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Jenkins Coverage | Shields.io

**URL:** https://shields.io/badges/jenkins-coverage

**Contents:**
- Jenkins Coverage

We support coverage metrics from a variety of Jenkins plugins:

Possible values: [jacoco, cobertura, apiv1, apiv4]

Example: https://jenkins-2.sse.uni-hildesheim.de/job/Teaching_SubmissionCheck

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub commit status | Shields.io

**URL:** https://shields.io/badges/git-hub-commit-status

**Contents:**
- GitHub commit status

Displays the status of a tag, commit, or branch, as reported by the Commit Status API.

Note: Nowadays, GitHub Actions and many third party integrations report state via the Checks API. If this badge does not show expected values, please try out our corresponding Check Runs badge instead. You can read more about status checks in the GitHub documentation.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: 91b108d4b7359b2f8794a4614c11cb1157dc9fff

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Maven Central Version | Shields.io

**URL:** https://shields.io/badges/maven-central-version

**Contents:**
- Maven Central Version

Maven Central Version

Example: com.google.guava

Possible values: [highestVersion, releaseProperty, latestProperty]

The strategy used to determine the version that will be shown

Example: highestVersion

The filter param can be used to apply a filter to the project's versions before selecting the latest from the list. Two constructs are available: * is a wildcard matching zero or more characters, and if the pattern starts with a !, the whole pattern is negated.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PyPI - Python Version | Shields.io

**URL:** https://shields.io/badges/py-pi-python-version

**Contents:**
- PyPI - Python Version

PyPI - Python Version

Example: https://pypi.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AUR License | Shields.io

**URL:** https://shields.io/badges/aur-license

**Contents:**
- AUR License

Arch linux User Repository License

Example: android-studio

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Libraries.io dependency status for GitHub repo | Shields.io

**URL:** https://shields.io/badges/libraries-io-dependency-status-for-git-hub-repo

**Contents:**
- Libraries.io dependency status for GitHub repo

Libraries.io dependency status for GitHub repo

Example: phoenixframework

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Plugin Stars | Shields.io

**URL:** https://shields.io/badges/word-press-plugin-stars

**Contents:**
- WordPress Plugin Stars

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PyPI - Wheel | Shields.io

**URL:** https://shields.io/badges/py-pi-wheel

**Contents:**
- PyPI - Wheel

Example: https://pypi.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Compatible versions (plugins on Ore) | Shields.io

**URL:** https://shields.io/badges/compatible-versions-plugins-on-ore

**Contents:**
- Compatible versions (plugins on Ore)

Ore is a Minecraft package repository.

Your Plugin ID is the name of your plugin in lowercase, without any spaces or dashes.

Example: https://ore.spongepowered.org/Erigitic/Total-Economy - Here the Plugin ID is totaleconomy.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Test Execution Time (branch) | Shields.io

**URL:** https://shields.io/badges/sonar-test-execution-time-branch

**Contents:**
- Sonar Test Execution Time (branch)

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Revolt | Shields.io

**URL:** https://shields.io/badges/revolt

**Contents:**
- Revolt

The Revolt badge requires an INVITE CODE to access the Revolt API, which can be located at the end of the invitation url.

For example, both https://app.revolt.chat/invite/01F7ZSBSFHQ8TA81725KQCSDDP and https://rvlt.gg/01F7ZSBSFHQ8TA81725KQCSDDP contains an invite code of 01F7ZSBSFHQ8TA81725KQCSDDP.

Example: 01F7ZSBSFHQ8TA81725KQCSDDP

Example: https://api.revolt.chat

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Factorio Mod Portal factorio versions | Shields.io

**URL:** https://shields.io/badges/factorio-mod-portal-factorio-versions

**Contents:**
- Factorio Mod Portal factorio versions

Factorio Mod Portal factorio versions

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub commit merge status | Shields.io

**URL:** https://shields.io/badges/git-hub-commit-merge-status

**Contents:**
- GitHub commit merge status

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: 5d4ab86b1b5ddfb3c4a70a70bd19932c52603b8c

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ore License | Shields.io

**URL:** https://shields.io/badges/ore-license

**Contents:**
- Ore License

Ore is a Minecraft package repository.

Your Plugin ID is the name of your plugin in lowercase, without any spaces or dashes.

Example: https://ore.spongepowered.org/Erigitic/Total-Economy - Here the Plugin ID is totaleconomy.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Codacy coverage (branch) | Shields.io

**URL:** https://shields.io/badges/codacy-coverage-branch

**Contents:**
- Codacy coverage (branch)

Codacy coverage (branch)

Example: 84c0a068ce9349f2bcaa07b5977bd932

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AUR Votes | Shields.io

**URL:** https://shields.io/badges/aur-votes

**Contents:**
- AUR Votes

Arch linux User Repository Votes

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Mastodon Follow | Shields.io

**URL:** https://shields.io/badges/mastodon-follow

**Contents:**
- Mastodon Follow

To find your user id, you can make a request to https://your.mastodon.server/api/v1/accounts/lookup?acct=yourusername.

Example: mastodon.social

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub contributors | Shields.io

**URL:** https://shields.io/badges/git-hub-contributors

**Contents:**
- GitHub contributors

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Note: Co-authors are not included in the count due to endpoint limitations.

Possible values: [contributors, contributors-anon]

contributors-anon includes anonymous commits, whereas contributors excludes them.

Example: contributors

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## jsDelivr hits (GitHub) | Shields.io

**URL:** https://shields.io/badges/js-delivr-hits-git-hub

**Contents:**
- jsDelivr hits (GitHub)

jsDelivr hits (GitHub)

Possible values: [hd, hw, hm, hy]

Hits per Day, Week, Month or Year

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM Downloads | Shields.io

**URL:** https://shields.io/badges/npm-downloads

**Contents:**
- NPM Downloads

Possible values: [dw, dm, dy, d18m]

Downloads in the last Week, Month, Year, or 18 Months

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub tag status | Shields.io

**URL:** https://shields.io/badges/git-hub-tag-status

**Contents:**
- GitHub tag status

Displays the status of a tag, commit, or branch, as reported by the Commit Status API.

Note: Nowadays, GitHub Actions and many third party integrations report state via the Checks API. If this badge does not show expected values, please try out our corresponding Check Runs badge instead. You can read more about status checks in the GitHub documentation.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Terraform Provider Downloads | Shields.io

**URL:** https://shields.io/badges/terraform-provider-downloads

**Contents:**
- Terraform Provider Downloads

Terraform Registry is an interactive resource for discovering a wide selection of integrations (providers), configuration packages (modules), and security rules (policies) for use with Terraform.

Possible values: [dw, dm, dy, dt]

Weekly, Monthly, Yearly or Total downloads

The provider ID can be found using https://registry.terraform.io/v2/providers/{namespace}/{name}

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PowerShell Gallery Version | Shields.io

**URL:** https://shields.io/badges/power-shell-gallery-version

**Contents:**
- PowerShell Gallery Version

PowerShell Gallery Version

Example: Azure.Storage

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gradle Plugin Portal Version | Shields.io

**URL:** https://shields.io/badges/gradle-plugin-portal-version

**Contents:**
- Gradle Plugin Portal Version

Gradle Plugin Portal Version

Example: com.gradle.plugin-publish

Possible values: [highestVersion, releaseProperty, latestProperty]

The strategy used to determine the version that will be shown

Example: highestVersion

The filter param can be used to apply a filter to the project's versions before selecting the latest from the list. Two constructs are available: * is a wildcard matching zero or more characters, and if the pattern starts with a !, the whole pattern is negated.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## npm bundle size (scoped) | Shields.io

**URL:** https://shields.io/badges/npm-bundle-size-scoped

**Contents:**
- npm bundle size (scoped)

Bundlephobia lets you understand the size of a javascript package from NPM before it becomes a part of your bundle.

Possible values: [min, minzip]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Pipenv locked dev dependency version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-pipenv-locked-dev-dependency-version-branch

**Contents:**
- GitHub Pipenv locked dev dependency version (branch)

Pipenv is a dependency manager for Python which manages a virtualenv for projects. It adds/removes packages from your Pipfile as you install/uninstall packages and generates the ever-important Pipfile.lock, which can be checked in to source control in order to produce deterministic builds.

The GitHub Pipenv badges are intended for applications using Pipenv which are hosted on GitHub.

When Pipfile.lock is checked in, the GitHub Pipenv locked dependency version badge displays the locked version of a dependency listed in [packages] or [dev-packages] (or any of their transitive dependencies).

Usually a Python version is specified in the Pipfile, which pipenv lock then places in Pipfile.lock. The GitHub Pipenv Python version badge displays that version.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: rq-dashboard-on-heroku

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub repo file or directory count (in path) | Shields.io

**URL:** https://shields.io/badges/git-hub-repo-file-or-directory-count-in-path

**Contents:**
- GitHub repo file or directory count (in path)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [dir, file]

Entity to count: directories or files. If not specified, both files and directories are counted. GitHub API has an upper limit of 1,000 files for a directory. If a directory contains files above the limit, the badge will show an inaccurate count.

Filter to files of type. Specify the extension without a leading dot. For instance for .js extension pass js. This param is only applicable if type is file

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Dependent repos (via libraries.io), scoped npm package | Shields.io

**URL:** https://shields.io/badges/dependent-repos-via-libraries-io-scoped-npm-package

**Contents:**
- Dependent repos (via libraries.io), scoped npm package

Dependent repos (via libraries.io), scoped npm package

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ecologi (Carbon Offset) | Shields.io

**URL:** https://shields.io/badges/ecologi-carbon-offset

**Contents:**
- Ecologi (Carbon Offset)

Ecologi (Carbon Offset)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CircleCI (branch) | Shields.io

**URL:** https://shields.io/badges/circle-ci-branch

**Contents:**
- CircleCI (branch)

Possible values: [github, gh, bitbucket, bb]

Example: node-csgo-parser

You may specify an optional token to get the status for a private repository.

If you need to use a token, please use a Project Token and only assign your token the 'Status' permission. Never use a Personal Token as they grant full read write permissions to your projects.

For more information about managing Circle CI tokens, please read this article.

Example: abc123def456

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitea Release | Shields.io

**URL:** https://shields.io/badges/gitea-release

**Contents:**
- Gitea Release

By default this badge looks for repositories on gitea.com. To specify another instance like codeberg, forgejo or a self-hosted instance, use the gitea_url query param.

Example: https://gitea.com

Possible values: [date, semver]

Possible values: [tag, release]

Possible values: [created_at, published_at]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CPAN License | Shields.io

**URL:** https://shields.io/badges/cpan-license

**Contents:**
- CPAN License

CPAN is a package registry for Perl

Example: Config-Augeas

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gem Downloads (for specified version) | Shields.io

**URL:** https://shields.io/badges/gem-downloads-for-specified-version

**Contents:**
- Gem Downloads (for specified version)

Ruby Gems is a registry for ruby libraries

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Spiget Rating | Shields.io

**URL:** https://shields.io/badges/spiget-rating

**Contents:**
- Spiget Rating

Spiget holds information about SpigotMC Resources, Plugins and Authors.

You can find your resource ID in the url for your resource page.

Example: https://www.spigotmc.org/resources/essentialsx.9089/ - Here the Resource ID is 9089.

Possible values: [rating, stars]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Read the Docs | Shields.io

**URL:** https://shields.io/badges/read-the-docs

**Contents:**
- Read the Docs

ReadTheDocs is a hosting service for documentation.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM Last Update | Shields.io

**URL:** https://shields.io/badges/npm-last-update

**Contents:**
- NPM Last Update

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Pipenv locked dev dependency version | Shields.io

**URL:** https://shields.io/badges/git-hub-pipenv-locked-dev-dependency-version

**Contents:**
- GitHub Pipenv locked dev dependency version

Pipenv is a dependency manager for Python which manages a virtualenv for projects. It adds/removes packages from your Pipfile as you install/uninstall packages and generates the ever-important Pipfile.lock, which can be checked in to source control in order to produce deterministic builds.

The GitHub Pipenv badges are intended for applications using Pipenv which are hosted on GitHub.

When Pipfile.lock is checked in, the GitHub Pipenv locked dependency version badge displays the locked version of a dependency listed in [packages] or [dev-packages] (or any of their transitive dependencies).

Usually a Python version is specified in the Pipfile, which pipenv lock then places in Pipfile.lock. The GitHub Pipenv Python version badge displays that version.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: rq-dashboard-on-heroku

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub R package version | Shields.io

**URL:** https://shields.io/badges/git-hub-r-package-version

**Contents:**
- GitHub R package version

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: mixOmicsTeam

The filename param can be used to specify the path to DESCRIPTION. By default, we look for DESCRIPTION in the repo root

Example: subdirectory/DESCRIPTION

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Testspace tests count | Shields.io

**URL:** https://shields.io/badges/testspace-tests-count

**Contents:**
- Testspace tests count

Testspace tests count

Possible values: [total, passed, failed, skipped, errored, untested]

Example: swellaby:testspace-sample

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Keybase BTC | Shields.io

**URL:** https://shields.io/badges/keybase-btc

**Contents:**
- Keybase BTC

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Node LTS (with tag) | Shields.io

**URL:** https://shields.io/badges/node-lts-with-tag

**Contents:**
- Node LTS (with tag)

This badge indicates whether the package supports all LTS node versions.

The node version support is retrieved from the engines.node section in package.json.

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Hangar Stars | Shields.io

**URL:** https://shields.io/badges/hangar-stars

**Contents:**
- Hangar Stars

Hangar is a plugin repository for the Paper, Waterfall and Folia platforms.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Actions Workflow Status | Shields.io

**URL:** https://shields.io/badges/git-hub-actions-workflow-status

**Contents:**
- GitHub Actions Workflow Status

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: unit-tests.yml

See GitHub Actions Events that trigger workflows for allowed values.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CRAN/METACRAN License | Shields.io

**URL:** https://shields.io/badges/cran-metacran-license

**Contents:**
- CRAN/METACRAN License

CRAN/METACRAN License

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Tests | Shields.io

**URL:** https://shields.io/badges/sonar-tests

**Contents:**
- Sonar Tests

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

You may change the "passed", "failed" and "skipped" text on this badge by supplying query parameters &passed_label=, &failed_label= and &skipped_label= respectively.

For example, if you want to use a different terminology:

?passed_label=good&failed_label=bad&skipped_label=n%2Fa

?compact_message&passed_label=💃&failed_label=🤦‍♀️&skipped_label=🤷

There is also a &compact_message query parameter, which will default to displaying ✔, ✘ and ➟, separated by a horizontal bar |.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Weblate user statistic | Shields.io

**URL:** https://shields.io/badges/weblate-user-statistic

**Contents:**
- Weblate user statistic

Weblate is an web-based tool for translation and internationalization

Possible values: [translations, suggestions, languages, uploads, comments]

Example: translations

Example: https://hosted.weblate.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Pub Likes | Shields.io

**URL:** https://shields.io/badges/pub-likes

**Contents:**
- Pub Likes

Pub is a package registry for Dart and Flutter.

This badge shows a measure of how many developers have liked a package. This provides a raw measure of the overall sentiment of a package from peer developers.

Example: analysis_options

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## UptimeObserver uptime ratio (7 days) | Shields.io

**URL:** https://shields.io/badges/uptime-observer-uptime-ratio-7-days

**Contents:**
- UptimeObserver uptime ratio (7 days)

UptimeObserver uptime ratio (7 days)

Example: 33Zw1rnH6veb4OLcskqvj6g9Lj4tnyxZ41

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CPAN Version | Shields.io

**URL:** https://shields.io/badges/cpan-version

**Contents:**
- CPAN Version

CPAN is a package registry for Perl

Example: Config-Augeas

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Docker Image Version | Shields.io

**URL:** https://shields.io/badges/docker-image-version

**Contents:**
- Docker Image Version

Possible values: [date, semver]

If not specified, the default is date

Possible values: [amd64, arm, arm64, s390x, 386, ppc64, ppc64le, wasm, mips, mipsle, mips64, mips64le, riscv64, loong64]

If not specified, the default is amd64

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## JetBrains ReSharper plugins Downloads | Shields.io

**URL:** https://shields.io/badges/jet-brains-re-sharper-plugins-downloads

**Contents:**
- JetBrains ReSharper plugins Downloads

JetBrains ReSharper plugins Downloads

Example: StyleCop.StyleCop

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ore Category | Shields.io

**URL:** https://shields.io/badges/ore-category

**Contents:**
- Ore Category

Ore is a Minecraft package repository.

Your Plugin ID is the name of your plugin in lowercase, without any spaces or dashes.

Example: https://ore.spongepowered.org/Erigitic/Total-Economy - Here the Plugin ID is totaleconomy.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Greasy Fork Downloads | Shields.io

**URL:** https://shields.io/badges/greasy-fork-downloads

**Contents:**
- Greasy Fork Downloads

Greasy Fork Downloads

Possible values: [dt, dd]

total downloads or daily downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub R package version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-r-package-version-branch

**Contents:**
- GitHub R package version (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: mixOmicsTeam

The filename param can be used to specify the path to DESCRIPTION. By default, we look for DESCRIPTION in the repo root

Example: subdirectory/DESCRIPTION

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sourceforge Open Tickets | Shields.io

**URL:** https://shields.io/badges/sourceforge-open-tickets

**Contents:**
- Sourceforge Open Tickets

Sourceforge Open Tickets

Possible values: [bugs, feature-requests]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Eclipse Marketplace Last Update | Shields.io

**URL:** https://shields.io/badges/eclipse-marketplace-last-update

**Contents:**
- Eclipse Marketplace Last Update

Eclipse Marketplace Last Update

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Puppet Forge endorsement | Shields.io

**URL:** https://shields.io/badges/puppet-forge-endorsement

**Contents:**
- Puppet Forge endorsement

Puppet Forge endorsement

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Open VSX Downloads (version) | Shields.io

**URL:** https://shields.io/badges/open-vsx-downloads-version

**Contents:**
- Open VSX Downloads (version)

Open VSX (OVSX) is a registry of extensions for VS Code compatible editors.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## ClearlyDefined Score | Shields.io

**URL:** https://shields.io/badges/clearly-defined-score

**Contents:**
- ClearlyDefined Score

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Matrix | Shields.io

**URL:** https://shields.io/badges/matrix

**Contents:**
- Matrix

In order for this badge to work, the host of your room must allow guest accounts or dummy accounts to register, and the room must be world readable (chat history visible to anyone).

Alternatively access via the experimental summary endpoint (MSC3266) can be configured with the query parameter fetchMode for less server load and better performance, if supported by the homeserver For the matrix.org homeserver fetchMode is hard-coded to summary.

The following steps will show you how to setup the badge URL using the Element Matrix client.

Some Matrix homeservers don't hold a server name matching where they live (e.g. if the homeserver example.com that created the room alias #mysuperroom:example.com lives at matrix.example.com).

If that is the case of the homeserver that created the room alias used for generating the badge, you will need to add the server's FQDN (fully qualified domain name) as a query parameter.

The final badge URL should then look something like this /matrix/mysuperroom:example.com.svg?server_fqdn=matrix.example.com.

Example: twim:matrix.org

Possible values: [guest, summary]

guest configures guest authentication while summary configures usage of the experimental "summary" endpoint (MSC3266). If not specified, the default fetch mode is guest (except for matrix.org).

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Flathub Version | Shields.io

**URL:** https://shields.io/badges/flathub-version

**Contents:**
- Flathub Version

Example: org.mozilla.firefox

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Modrinth Version | Shields.io

**URL:** https://shields.io/badges/modrinth-version

**Contents:**
- Modrinth Version

You can use your project slug, or the project ID. The ID can be found in the 'Technical information' section of your Modrinth page.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub package.json version | Shields.io

**URL:** https://shields.io/badges/git-hub-package-json-version

**Contents:**
- GitHub package.json version

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: badge-maker/package.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitea Issues | Shields.io

**URL:** https://shields.io/badges/gitea-issues

**Contents:**
- Gitea Issues

By default this badge looks for repositories on gitea.com. To specify another instance like codeberg, forgejo or a self-hosted instance, use the gitea_url query param.

Possible values: [all, all-raw, open, open-raw, closed, closed-raw]

Example: https://gitea.com

If you want to use multiple labels, you can use a comma (,) to separate them, e.g. foo,bar

Example: test,failure::new

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Plugin Last Updated | Shields.io

**URL:** https://shields.io/badges/word-press-plugin-last-updated

**Contents:**
- WordPress Plugin Last Updated

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AUR Version | Shields.io

**URL:** https://shields.io/badges/aur-version

**Contents:**
- AUR Version

Arch linux User Repository Version

Example: visual-studio-code-bin

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## JetBrains ReSharper plugins Version | Shields.io

**URL:** https://shields.io/badges/jet-brains-re-sharper-plugins-version

**Contents:**
- JetBrains ReSharper plugins Version

JetBrains ReSharper plugins Version

Example: StyleCop.StyleCop

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Pipenv locked Python version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-pipenv-locked-python-version-branch

**Contents:**
- GitHub Pipenv locked Python version (branch)

Pipenv is a dependency manager for Python which manages a virtualenv for projects. It adds/removes packages from your Pipfile as you install/uninstall packages and generates the ever-important Pipfile.lock, which can be checked in to source control in order to produce deterministic builds.

The GitHub Pipenv badges are intended for applications using Pipenv which are hosted on GitHub.

When Pipfile.lock is checked in, the GitHub Pipenv locked dependency version badge displays the locked version of a dependency listed in [packages] or [dev-packages] (or any of their transitive dependencies).

Usually a Python version is specified in the Pipfile, which pipenv lock then places in Pipfile.lock. The GitHub Pipenv Python version badge displays that version.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: rq-dashboard-on-heroku

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Dynamic Regex Badge | Shields.io

**URL:** https://shields.io/badges/dynamic-regex-badge

**Contents:**
- Dynamic Regex Badge

⚠️ Experimental: This badge is considered experimental and may change or be removed at any time.

This badge will extract text from a file using re2 (a subset of regex: https://github.com/google/re2). The main use-case is to extract values from unstructured plain-text files. For example: if a file contains a line like version - 2.4 you can extract the value 2.4 by using a search regex of version - (.*) and $1 as replacement.

Full Syntax documentation here: https://github.com/google/re2/wiki/Syntax

The URL to a file to search. The full raw content will be used as the search string.

Example: https://raw.githubusercontent.com/badges/shields/refs/heads/master/README.md

A re2 expression that will be used to extract data from the document. Only the first matched text will be returned.

Example: Every (.*?) it serves (?<amount>.*?) images

A replacement string that will be used as the replacement of the search regex. Use $$ to escape a $ sign, $n to specify the nth matched group, $<name> for a named group, etc. If empty (default), no replacement will be done and the full matched text will be shown.

Example: $<amount>/$1

Flags to be used when creating the regex: i = case-insensitive, m = multi-line mode, s = dot matches linebreaks. None by default.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Cocoapods License | Shields.io

**URL:** https://shields.io/badges/cocoapods-license

**Contents:**
- Cocoapods License

Example: AFNetworking

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub License | Shields.io

**URL:** https://shields.io/badges/git-hub-license

**Contents:**
- GitHub License

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Snapcraft Last Update | Shields.io

**URL:** https://shields.io/badges/snapcraft-last-update

**Contents:**
- Snapcraft Last Update

Snapcraft Last Update

Architecture, when not specified, this will default to amd64.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub pull request check contexts | Shields.io

**URL:** https://shields.io/badges/git-hub-pull-request-check-contexts

**Contents:**
- GitHub pull request check contexts

Displays the status of a pull request, as reported by the Commit Status API.

Note: Nowadays, GitHub Actions and many third party integrations report state via the Checks API. If this badge does not show expected values, please try out one of our Check Runs badges instead. You can read more about status checks in the GitHub documentation.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub lerna version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-lerna-version-branch

**Contents:**
- GitHub lerna version (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Twitch Status | Shields.io

**URL:** https://shields.io/badges/twitch-status

**Contents:**
- Twitch Status

Example: andyonthewings

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Hex.pm License | Shields.io

**URL:** https://shields.io/badges/hex-pm-license

**Contents:**
- Hex.pm License

Hex.pm is a package registry for Erlang

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub manifest.json dynamic | Shields.io

**URL:** https://shields.io/badges/git-hub-manifest-json-dynamic

**Contents:**
- GitHub manifest.json dynamic

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: sindresorhus

Example: show-all-github-issues

Example: extension/manifest.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## VPM Package Version | Shields.io

**URL:** https://shields.io/badges/vpm-package-version

**Contents:**
- VPM Package Version

VPM is the VRChat Package Manager

Example: com.vrchat.udonsharp

Example: https://packages.vrchat.com/curated?download

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Fedora package | Shields.io

**URL:** https://shields.io/badges/fedora-package

**Contents:**
- Fedora package

See mdapi docs for information on valid branches.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub branch count | Shields.io

**URL:** https://shields.io/badges/git-hub-branch-count

**Contents:**
- GitHub branch count

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Homebrew Cask Version | Shields.io

**URL:** https://shields.io/badges/homebrew-cask-version

**Contents:**
- Homebrew Cask Version

Homebrew Cask Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Elm package | Shields.io

**URL:** https://shields.io/badges/elm-package

**Contents:**
- Elm package

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## OPM Version | Shields.io

**URL:** https://shields.io/badges/opm-version

**Contents:**
- OPM Version

Example: lua-resty-lrucache

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab Last Commit | Shields.io

**URL:** https://shields.io/badges/git-lab-last-commit

**Contents:**
- GitLab Last Commit

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

ref can be filled with the name of a branch, tag or revision range of the repository.

Example: gitlab-org/gitlab

Example: https://gitlab.com

File path to resolve the last commit for.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Open VSX Rating | Shields.io

**URL:** https://shields.io/badges/open-vsx-rating

**Contents:**
- Open VSX Rating

Open VSX (OVSX) is a registry of extensions for VS Code compatible editors.

Possible values: [rating, stars]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## JetBrains Plugin Rating | Shields.io

**URL:** https://shields.io/badges/jet-brains-plugin-rating

**Contents:**
- JetBrains Plugin Rating

JetBrains Plugin Rating

Possible values: [rating, stars]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Downloads (all assets, all releases) | Shields.io

**URL:** https://shields.io/badges/git-hub-downloads-all-assets-all-releases

**Contents:**
- GitHub Downloads (all assets, all releases)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Downloads (all assets, latest release) | Shields.io

**URL:** https://shields.io/badges/git-hub-downloads-all-assets-latest-release

**Contents:**
- GitHub Downloads (all assets, latest release)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [downloads, downloads-pre]

downloads including or excluding pre-releases

Possible values: [date, semver]

Method used to determine latest release. Default: date

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## npm bundle size (version) | Shields.io

**URL:** https://shields.io/badges/npm-bundle-size-version

**Contents:**
- npm bundle size (version)

Bundlephobia lets you understand the size of a javascript package from NPM before it becomes a part of your bundle.

Possible values: [min, minzip]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer build (GitLab) with branch | Shields.io

**URL:** https://shields.io/badges/scrutinizer-build-git-lab-with-branch

**Contents:**
- Scrutinizer build (GitLab) with branch

Scrutinizer build (GitLab) with branch

Example: propertywindow

Example: propertywindow

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Packagist Downloads | Shields.io

**URL:** https://shields.io/badges/packagist-downloads

**Contents:**
- Packagist Downloads

Packagist is a registry for PHP packages which can be installed with Composer.

Displayed data may be slightly outdated. Due to performance reasons, data fetched from packagist JSON API is cached for twelve hours on packagist infrastructure. For more information please refer to official packagist documentation.

Possible values: [dd, dm, dt]

Daily, Monthly, or Total downloads

Note that only network-accessible packagist.org and other self-hosted Packagist instances are supported.

Example: https://packagist.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Drone | Shields.io

**URL:** https://shields.io/badges/drone

**Contents:**
- Drone

Example: https://drone.shields.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Libraries.io SourceRank | Shields.io

**URL:** https://shields.io/badges/libraries-io-source-rank

**Contents:**
- Libraries.io SourceRank

Libraries.io SourceRank

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PyPI - Types | Shields.io

**URL:** https://shields.io/badges/py-pi-types

**Contents:**
- PyPI - Types

Type information provided by the package, as indicated by the presence of the Typing :: Typed and Typing :: Stubs Only classifiers in the package metadata

Example: https://pypi.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## JitPack | Shields.io

**URL:** https://shields.io/badges/jit-pack

**Contents:**
- JitPack

Example: com.github.jitpack

Example: maven-simple

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Spiget tested server versions | Shields.io

**URL:** https://shields.io/badges/spiget-tested-server-versions

**Contents:**
- Spiget tested server versions

Spiget holds information about SpigotMC Resources, Plugins and Authors.

You can find your resource ID in the url for your resource page.

Example: https://www.spigotmc.org/resources/essentialsx.9089/ - Here the Resource ID is 9089.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub file size in bytes | Shields.io

**URL:** https://shields.io/badges/git-hub-file-size-in-bytes

**Contents:**
- GitHub file size in bytes

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: build/phaser-craft.min.js

Can be a branch, a tag or a commit hash.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Vcpkg Version | Shields.io

**URL:** https://shields.io/badges/vcpkg-version

**Contents:**
- Vcpkg Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Galaxy Toolshed - Tool Version | Shields.io

**URL:** https://shields.io/badges/galaxy-toolshed-tool-version

**Contents:**
- Galaxy Toolshed - Tool Version

Galaxy Toolshed - Tool Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## freeCodeCamp points | Shields.io

**URL:** https://shields.io/badges/free-code-camp-points

**Contents:**
- freeCodeCamp points

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Python Version from PEP 621 TOML | Shields.io

**URL:** https://shields.io/badges/python-version-from-pep-621-toml

**Contents:**
- Python Version from PEP 621 TOML

Shows the required python version for a package based on the values in the requires-python field in PEP 621 compliant pyproject.toml

a URL of the toml is required, please note that when linking to files in github or similar sites, provide URL to raw file, for example:

Use https://raw.githubusercontent.com/numpy/numpy/main/pyproject.toml

And not https://github.com/numpy/numpy/blob/main/pyproject.toml

Example: https://raw.githubusercontent.com/numpy/numpy/main/pyproject.toml

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Theme: Required WP Version | Shields.io

**URL:** https://shields.io/badges/word-press-theme-required-wp-version

**Contents:**
- WordPress Theme: Required WP Version

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Example: twentytwenty

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Test Count | Shields.io

**URL:** https://shields.io/badges/sonar-test-count

**Contents:**
- Sonar Test Count

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Possible values: [total_tests, skipped_tests, test_failures, test_errors]

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Puppet Forge version | Shields.io

**URL:** https://shields.io/badges/puppet-forge-version

**Contents:**
- Puppet Forge version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub lerna version | Shields.io

**URL:** https://shields.io/badges/git-hub-lerna-version

**Contents:**
- GitHub lerna version

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Plugin Active Installs | Shields.io

**URL:** https://shields.io/badges/word-press-plugin-active-installs

**Contents:**
- WordPress Plugin Active Installs

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## nycrc config on GitHub | Shields.io

**URL:** https://shields.io/badges/nycrc-config-on-git-hub

**Contents:**
- nycrc config on GitHub

Create a code coverage badge, based on thresholds stored in a .nycrc config file on GitHub.

Possible values: [branches, lines, functions]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## docs.rs | Shields.io

**URL:** https://shields.io/badges/docs-rs

**Contents:**
- docs.rs

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Raycast extension downloads count | Shields.io

**URL:** https://shields.io/badges/raycast-extension-downloads-count

**Contents:**
- Raycast extension downloads count

Raycast extension downloads count

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PyPI - Downloads | Shields.io

**URL:** https://shields.io/badges/py-pi-downloads

**Contents:**
- PyPI - Downloads

Python package downloads from pypistats

Possible values: [dd, dw, dm]

Daily, Weekly, or Monthly downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Dependent repos (via libraries.io) | Shields.io

**URL:** https://shields.io/badges/dependent-repos-via-libraries-io

**Contents:**
- Dependent repos (via libraries.io)

Dependent repos (via libraries.io)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Debian package | Shields.io

**URL:** https://shields.io/badges/debian-package

**Contents:**
- Debian package

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NodePing status | Shields.io

**URL:** https://shields.io/badges/node-ping-status

**Contents:**
- NodePing status

Example: jkiwn052-ntpp-4lbb-8d45-ihew6d9ucoei

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Puppet Forge feedback score | Shields.io

**URL:** https://shields.io/badges/puppet-forge-feedback-score

**Contents:**
- Puppet Forge feedback score

Puppet Forge feedback score

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## JIRA issue | Shields.io

**URL:** https://shields.io/badges/jira-issue

**Contents:**
- JIRA issue

Example: https://issues.apache.org/jira

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Eclipse Marketplace Favorites | Shields.io

**URL:** https://shields.io/badges/eclipse-marketplace-favorites

**Contents:**
- Eclipse Marketplace Favorites

Eclipse Marketplace Favorites

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## iTunes App Store | Shields.io

**URL:** https://shields.io/badges/i-tunes-app-store

**Contents:**
- iTunes App Store

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub number of milestones | Shields.io

**URL:** https://shields.io/badges/git-hub-number-of-milestones

**Contents:**
- GitHub number of milestones

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [open, closed, all]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub go.mod Go version | Shields.io

**URL:** https://shields.io/badges/git-hub-go-mod-go-version

**Contents:**
- GitHub go.mod Go version

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

The filename param can be used to specify the path to go.mod. By default, we look for go.mod in the repo root

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Netlify | Shields.io

**URL:** https://shields.io/badges/netlify

**Contents:**
- Netlify

To locate your project id, visit your project settings, scroll to "Status badges" under "General", and copy the ID between "/api/v1/badges/" and "/deploy-status" in the code sample

Example: e6d5a4e0-dee1-4261-833e-2f47f509c68f

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Homebrew Cask Downloads | Shields.io

**URL:** https://shields.io/badges/homebrew-cask-downloads

**Contents:**
- Homebrew Cask Downloads

Homebrew Cask Downloads

Possible values: [dm, dq, dy]

Monthly, Quarterly or Yearly downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bower Version | Shields.io

**URL:** https://shields.io/badges/bower-version

**Contents:**
- Bower Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub package.json dev/peer/optional dependency version | Shields.io

**URL:** https://shields.io/badges/git-hub-package-json-dev-peer-optional-dependency-version

**Contents:**
- GitHub package.json dev/peer/optional dependency version

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [dev, peer, optional]

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: packages/gatsby-cli/package.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## TeamCity Full Build Status | Shields.io

**URL:** https://shields.io/badges/team-city-full-build-status

**Contents:**
- TeamCity Full Build Status

TeamCity Full Build Status

Example: https://teamcity.jetbrains.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Eclipse Marketplace Downloads | Shields.io

**URL:** https://shields.io/badges/eclipse-marketplace-downloads

**Contents:**
- Eclipse Marketplace Downloads

Eclipse Marketplace Downloads

Possible values: [dm, dt]

Monthly or Total downloads

Example: planet-themes

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Vaadin Directory Rating | Shields.io

**URL:** https://shields.io/badges/vaadin-directory-rating

**Contents:**
- Vaadin Directory Rating

Vaadin Directory Rating

Possible values: [star, stars, rating]

Example: vaadinvaadin-grid

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer build (GitHub/Bitbucket) | Shields.io

**URL:** https://shields.io/badges/scrutinizer-build-git-hub-bitbucket

**Contents:**
- Scrutinizer build (GitHub/Bitbucket)

Scrutinizer build (GitHub/Bitbucket)

Possible values: [g, b]

Platform: Either GitHub or Bitbucket

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## HackerNews User Karma | Shields.io

**URL:** https://shields.io/badges/hacker-news-user-karma

**Contents:**
- HackerNews User Karma

HackerNews User Karma

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Endpoint Badge | Shields.io

**URL:** https://shields.io/badges/endpoint-badge

**Contents:**
- Endpoint Badge
- Example JSON Endpoint Response
- Example Shields Response
- Schema

Using the endpoint badge, you can provide content for a badge through a JSON endpoint. The content can be prerendered, or generated on the fly. To strike a balance between responsiveness and bandwidth utilization on one hand, and freshness on the other, cache behavior is configurable, subject to the Shields minimum. The endpoint URL is provided to Shields through the query string. Shields fetches it and formats the badge.

The endpoint badge takes a single required query param: url, which is the URL to your JSON endpoint

Required. The left text, or the empty string to omit the left side of the badge. This can be overridden by the query string.

Default: lightgrey. The right color. Supports the eight named colors above, as well as hex, rgb, rgba, hsl, hsla and css named colors. This can be overridden by the query string.

Default: grey. The left color. This can be overridden by the query string.

Default: false. true to treat this as an error badge. This prevents the user from overriding the color. In the future, it may affect cache behavior.

Default: none. One of the simple-icons slugs. Can be overridden by the query string.

Default: none. Same meaning as the query string. Can be overridden by the query string. Only works for simple-icons logos.

Default: none. Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos only.

Default: flat. The default template to use. Can be overridden by the query string.

The URL to your JSON endpoint

Example: https://shields.redsparr0w.com/2473/monday

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Eclipse Marketplace Version | Shields.io

**URL:** https://shields.io/badges/eclipse-marketplace-version

**Contents:**
- Eclipse Marketplace Version

Eclipse Marketplace Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Flathub Downloads | Shields.io

**URL:** https://shields.io/badges/flathub-downloads

**Contents:**
- Flathub Downloads

Example: org.mozilla.firefox

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Downloads (all assets, specific tag) | Shields.io

**URL:** https://shields.io/badges/git-hub-downloads-all-assets-specific-tag

**Contents:**
- GitHub Downloads (all assets, specific tag)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab go.mod Go version (branch) | Shields.io

**URL:** https://shields.io/badges/git-lab-go-mod-go-version-branch

**Contents:**
- GitLab go.mod Go version (branch)

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Example: gitlab-runner

The filename param can be used to specify the path to go.mod. By default, we look for go.mod in the repo root

Example: https://gitlab.example.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM Collaborators | Shields.io

**URL:** https://shields.io/badges/npm-collaborators

**Contents:**
- NPM Collaborators

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Packagist Stars | Shields.io

**URL:** https://shields.io/badges/packagist-stars

**Contents:**
- Packagist Stars

Packagist is a registry for PHP packages which can be installed with Composer.

Displayed data may be slightly outdated. Due to performance reasons, data fetched from packagist JSON API is cached for twelve hours on packagist infrastructure. For more information please refer to official packagist documentation.

Note that only network-accessible packagist.org and other self-hosted Packagist instances are supported.

Example: https://packagist.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## X (formerly Twitter) URL | Shields.io

**URL:** https://shields.io/badges/x-formerly-twitter-url

**Contents:**
- X (formerly Twitter) URL

X (formerly Twitter) URL

Example: https://shields.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Packagist License | Shields.io

**URL:** https://shields.io/badges/packagist-license

**Contents:**
- Packagist License

Packagist is a registry for PHP packages which can be installed with Composer.

Note that only network-accessible packagist.org and other self-hosted Packagist instances are supported.

Example: https://packagist.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## npm bundle size | Shields.io

**URL:** https://shields.io/badges/npm-bundle-size

**Contents:**
- npm bundle size

Bundlephobia lets you understand the size of a javascript package from NPM before it becomes a part of your bundle.

Possible values: [min, minzip]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Thunderstore Downloads | Shields.io

**URL:** https://shields.io/badges/thunderstore-downloads

**Contents:**
- Thunderstore Downloads

The Thunderstore badges require a package's namespace and name.

Everything can be discerned from your package's URL. Thunderstore package URLs have a mostly consistent format:

For example: https://thunderstore.io/c/lethal-company/p/notnotnotswipez/MoreCompany/.

The 'default community', Risk of Rain 2, has an alternative URL:

Some communities use a 'subdomain' alternative URL, for example, Valheim:

Example: notnotnotswipez

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Uptime Robot ratio (7 days) | Shields.io

**URL:** https://shields.io/badges/uptime-robot-ratio-7-days

**Contents:**
- Uptime Robot ratio (7 days)

Uptime Robot ratio (7 days)

Example: m778918918-3e92c097147760ee39d02d36

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Node Current | Shields.io

**URL:** https://shields.io/badges/node-current

**Contents:**
- Node Current

This badge indicates whether the package supports the latest release of node.

The node version support is retrieved from the engines.node section in package.json.

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CodeFactor Grade (with branch) | Shields.io

**URL:** https://shields.io/badges/code-factor-grade-with-branch

**Contents:**
- CodeFactor Grade (with branch)

CodeFactor Grade (with branch)

Possible values: [github, bitbucket]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Hex.pm Downloads | Shields.io

**URL:** https://shields.io/badges/hex-pm-downloads

**Contents:**
- Hex.pm Downloads

Hex.pm is a package registry for Erlang

Possible values: [dd, dw, dt]

Daily, Weekly, or Total downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Hangar Views | Shields.io

**URL:** https://shields.io/badges/hangar-views

**Contents:**
- Hangar Views

Hangar is a plugin repository for the Paper, Waterfall and Folia platforms.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Static Badge | Shields.io

**URL:** https://shields.io/badges

**Contents:**
- Static Badge

The static badge accepts a single required path parameter which encodes either:

Label, message and color separated by a dash -. For example: - https://img.shields.io/badge/any_text-you_like-blue

Message and color only, separated by a dash -. For example: - https://img.shields.io/badge/just%20the%20message-8A2BE2

Examples of additional options:

Style: - https://img.shields.io/badge/build-passing-brightgreen?style=for-the-badge

Color (named): - https://img.shields.io/badge/coverage-95%25-orange

Logo: - https://img.shields.io/badge/github-repo-blue?logo=github

Label, (optional) message, and color. Separated by dashes

Example: build-passing-brightgreen

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Snapcraft License | Shields.io

**URL:** https://shields.io/badges/snapcraft-license

**Contents:**
- Snapcraft License

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Conan Center | Shields.io

**URL:** https://shields.io/badges/conan-center

**Contents:**
- Conan Center

Conan is a package manager for C++

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab Language Count | Shields.io

**URL:** https://shields.io/badges/git-lab-language-count

**Contents:**
- GitLab Language Count

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Example: gitlab-org/gitlab

Example: https://gitlab.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gem Total Downloads | Shields.io

**URL:** https://shields.io/badges/gem-total-downloads

**Contents:**
- Gem Total Downloads

Ruby Gems is a registry for ruby libraries

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## npms.io | Shields.io

**URL:** https://shields.io/badges/npms-io

**Contents:**
- npms.io

npms.io holds statistics for javascript packages.

Possible values: [final-score, maintenance-score, popularity-score, quality-score]

Example: maintenance-score

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Deps.rs Repository Dependencies | Shields.io

**URL:** https://shields.io/badges/deps-rs-repository-dependencies

**Contents:**
- Deps.rs Repository Dependencies

Deps.rs is a service that checks dependency security status in Rust projects.

Possible values: [github, gitlab, bitbucket, sourcehut, codeberg]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Cirrus CI - Default Branch Build Status | Shields.io

**URL:** https://shields.io/badges/cirrus-ci-default-branch-build-status

**Contents:**
- Cirrus CI - Default Branch Build Status

Cirrus CI - Default Branch Build Status

Example: build_docker

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub watchers | Shields.io

**URL:** https://shields.io/badges/git-hub-watchers

**Contents:**
- GitHub watchers

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ansible Role | Shields.io

**URL:** https://shields.io/badges/ansible-role

**Contents:**
- Ansible Role

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Quality Gate (branch) | Shields.io

**URL:** https://shields.io/badges/sonar-quality-gate-branch

**Contents:**
- Sonar Quality Gate (branch)

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Gist stars | Shields.io

**URL:** https://shields.io/badges/git-hub-gist-stars

**Contents:**
- GitHub Gist stars

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

This badge shows the number of stargazers for a gist. Gist id is accepted as input and 'gist not found' is returned if the gist is not found for the given gist id.

Example: 47a4d00457a92aa426dbd48a18776322

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Lemmy | Shields.io

**URL:** https://shields.io/badges/lemmy

**Contents:**
- Lemmy

Example: asklemmy@lemmy.ml

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## YouTube Channel Views | Shields.io

**URL:** https://shields.io/badges/you-tube-channel-views

**Contents:**
- YouTube Channel Views

Shields.io is committed to protecting the privacy of its users. We do not collect, store, or track any personal information or data returned by the YouTube API Services. Our service is designed to generate badges based on public YouTube data, and we do not retain any user-specific information:

By using the YouTube badge, you are:

Example: UC8butISFwT-Wl7EV0hUK0BQ

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Azure DevOps builds | Shields.io

**URL:** https://shields.io/badges/azure-dev-ops-builds

**Contents:**
- Azure DevOps builds

Azure Devops (formerly VSO, VSTS) is Microsoft Azure's CI/CD platform.

A badge requires three pieces of information: ORGANIZATION, PROJECT_ID and DEFINITION_ID.

To start, edit your build definition and look at the url:

Then use the Azure DevOps REST API to translate the PROJECT_NAME to a PROJECT_ID.

Navigate to https://dev.azure.com/ORGANIZATION/_apis/projects/PROJECT_NAME

Example: 8cf3ec0e-d0c2-4fcd-8206-ad204f254a96

Example: Successful Stage

Example: Successful Job

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Release | Shields.io

**URL:** https://shields.io/badges/git-hub-release

**Contents:**
- GitHub Release

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [date, semver]

The filter param can be used to apply a filter to the project's tag or release names before selecting the latest from the list. Two constructs are available: * is a wildcard matching zero or more characters, and if the pattern starts with a !, the whole pattern is negated.

Possible values: [tag, release]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub top language | Shields.io

**URL:** https://shields.io/badges/git-hub-top-language

**Contents:**
- GitHub top language

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer coverage (GitLab) with branch | Shields.io

**URL:** https://shields.io/badges/scrutinizer-coverage-git-lab-with-branch

**Contents:**
- Scrutinizer coverage (GitLab) with branch

Scrutinizer coverage (GitLab) with branch

Example: propertywindow

Example: propertywindow

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Packagist Dependency Version | Shields.io

**URL:** https://shields.io/badges/packagist-dependency-version

**Contents:**
- Packagist Dependency Version

Packagist is a registry for PHP packages which can be installed with Composer.

dependency can be a PHP package like twig/twig or a platform/extension like php or ext-xml

Note that only network-accessible packagist.org and other self-hosted Packagist instances are supported.

Example: https://packagist.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Pulsar Stargazers | Shields.io

**URL:** https://shields.io/badges/pulsar-stargazers

**Contents:**
- Pulsar Stargazers

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Mozilla Add-on Stars | Shields.io

**URL:** https://shields.io/badges/mozilla-add-on-stars

**Contents:**
- Mozilla Add-on Stars

addons.mozilla.org (AMO) publishes extensions for Mozilla Firefox

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Docker Pulls | Shields.io

**URL:** https://shields.io/badges/docker-pulls

**Contents:**
- Docker Pulls

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## docs.rs (with version) | Shields.io

**URL:** https://shields.io/badges/docs-rs-with-version

**Contents:**
- docs.rs (with version)

docs.rs (with version)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## MyGet Version (tenant) | Shields.io

**URL:** https://shields.io/badges/my-get-version-tenant

**Contents:**
- MyGet Version (tenant)

MyGet Version (tenant)

MyGet Tenant in the format name.myget

Example: vs-devcore.myget

Possible values: [v, vpre]

Latest stable version (v) or Latest version including prereleases (vpre).

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Travis (.com) | Shields.io

**URL:** https://shields.io/badges/travis-com

**Contents:**
- Travis (.com)

Example: ivandelabeldad

Example: rackian-gateway

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Plugin: Tested WP Version | Shields.io

**URL:** https://shields.io/badges/word-press-plugin-tested-wp-version

**Contents:**
- WordPress Plugin: Tested WP Version

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Mozilla Add-on Downloads | Shields.io

**URL:** https://shields.io/badges/mozilla-add-on-downloads

**Contents:**
- Mozilla Add-on Downloads

addons.mozilla.org (AMO) publishes extensions for Mozilla Firefox

Previously amo/d provided a “total downloads” badge. However, updates to the v3 API only give us weekly downloads. The route amo/d redirects to amo/dw.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Packagist Version | Shields.io

**URL:** https://shields.io/badges/packagist-version

**Contents:**
- Packagist Version

Packagist is a registry for PHP packages which can be installed with Composer.

Note that only network-accessible packagist.org and other self-hosted Packagist instances are supported.

Example: https://packagist.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## OBS package build status | Shields.io

**URL:** https://shields.io/badges/obs-package-build-status

**Contents:**
- OBS package build status

Open Build Service (OBS) is a generic system to build and distribute binary packages

Example: openSUSE:Tools

Example: https://api.opensuse.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub branch status | Shields.io

**URL:** https://shields.io/badges/git-hub-branch-status

**Contents:**
- GitHub branch status

Displays the status of a tag, commit, or branch, as reported by the Commit Status API.

Note: Nowadays, GitHub Actions and many third party integrations report state via the Checks API. If this badge does not show expected values, please try out our corresponding Check Runs badge instead. You can read more about status checks in the GitHub documentation.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Node Current (with tag) | Shields.io

**URL:** https://shields.io/badges/node-current-with-tag

**Contents:**
- Node Current (with tag)

This badge indicates whether the package supports the latest release of node.

The node version support is retrieved from the engines.node section in package.json.

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Conda Version | Shields.io

**URL:** https://shields.io/badges/conda-version

**Contents:**
- Conda Version

Possible values: [v, vn]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io License | Shields.io

**URL:** https://shields.io/badges/crates-io-license

**Contents:**
- Crates.io License

Crates.io is a package registry for Rust.

Example: rustc-serialize

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub commits difference between two branches/tags/commits | Shields.io

**URL:** https://shields.io/badges/git-hub-commits-difference-between-two-branches-tags-commits

**Contents:**
- GitHub commits difference between two branches/tags/commits

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Gist last commit | Shields.io

**URL:** https://shields.io/badges/git-hub-gist-last-commit

**Contents:**
- GitHub Gist last commit

Shows the latest commit to a GitHub Gist.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CRAN/METACRAN Version | Shields.io

**URL:** https://shields.io/badges/cran-metacran-version

**Contents:**
- CRAN/METACRAN Version

CRAN/METACRAN Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Steam Collection Files | Shields.io

**URL:** https://shields.io/badges/steam-collection-files

**Contents:**
- Steam Collection Files

Using a web browser, you can find the ID in the url here:

In the steam client you can simply just Right-Click and 'Copy Page URL' and follow the above step

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub code size in bytes | Shields.io

**URL:** https://shields.io/badges/git-hub-code-size-in-bytes

**Contents:**
- GitHub code size in bytes

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Galaxy Toolshed - Created Date | Shields.io

**URL:** https://shields.io/badges/galaxy-toolshed-created-date

**Contents:**
- Galaxy Toolshed - Created Date

Galaxy Toolshed - Created Date

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## TeamCity Simple Build Status | Shields.io

**URL:** https://shields.io/badges/team-city-simple-build-status

**Contents:**
- TeamCity Simple Build Status

TeamCity Simple Build Status

Example: IntelliJIdeaCe_JavaDecompilerEngineTests

Example: https://teamcity.jetbrains.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub branch check runs | Shields.io

**URL:** https://shields.io/badges/git-hub-branch-check-runs

**Contents:**
- GitHub branch check runs

The Check Runs service shows the status of GitHub action runs.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Azure DevOps builds (branch) | Shields.io

**URL:** https://shields.io/badges/azure-dev-ops-builds-branch

**Contents:**
- Azure DevOps builds (branch)

Azure Devops (formerly VSO, VSTS) is Microsoft Azure's CI/CD platform.

A badge requires three pieces of information: ORGANIZATION, PROJECT_ID and DEFINITION_ID.

To start, edit your build definition and look at the url:

Then use the Azure DevOps REST API to translate the PROJECT_NAME to a PROJECT_ID.

Navigate to https://dev.azure.com/ORGANIZATION/_apis/projects/PROJECT_NAME

Example: 8cf3ec0e-d0c2-4fcd-8206-ad204f254a96

Example: Successful Stage

Example: Successful Job

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Swagger Validator | Shields.io

**URL:** https://shields.io/badges/swagger-validator

**Contents:**
- Swagger Validator

Example: https://raw.githubusercontent.com/OAI/OpenAPI-Specification/c442afe06ec28443df0c69d01dc38c54968b246f/examples/v2.0/json/petstore-expanded.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bitbucket Pipelines | Shields.io

**URL:** https://shields.io/badges/bitbucket-pipelines

**Contents:**
- Bitbucket Pipelines

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Puppet Forge downloads | Shields.io

**URL:** https://shields.io/badges/puppet-forge-downloads

**Contents:**
- Puppet Forge downloads

Puppet Forge downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Downloads (specific asset, specific tag) | Shields.io

**URL:** https://shields.io/badges/git-hub-downloads-specific-asset-specific-tag

**Contents:**
- GitHub Downloads (specific asset, specific tag)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: atom-amd64.deb

Whether to display the asset name in the badge value

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Open Collective sponsors | Shields.io

**URL:** https://shields.io/badges/open-collective-sponsors

**Contents:**
- Open Collective sponsors

Open Collective sponsors

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub commits since latest release | Shields.io

**URL:** https://shields.io/badges/git-hub-commits-since-latest-release

**Contents:**
- GitHub commits since latest release

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

The include_prereleases, sort and filter params can be used to configure how we determine the latest version.

Example: SubtitleEdit

Example: subtitleedit

Possible values: [date, semver]

The filter param can be used to apply a filter to the project's tag or release names before selecting the latest from the list. Two constructs are available: * is a wildcard matching zero or more characters, and if the pattern starts with a !, the whole pattern is negated.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Voxel Shop Version | Shields.io

**URL:** https://shields.io/badges/voxel-shop-version

**Contents:**
- Voxel Shop Version

You can find your resource ID in the url for your product page.

Example: https://voxel.shop/product/323/polymart-plugin - Here the Resource ID is 323.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Chrome Web Store Last Updated | Shields.io

**URL:** https://shields.io/badges/chrome-web-store-last-updated

**Contents:**
- Chrome Web Store Last Updated

Our servers make requests to the Chrome Web Store from the US East region. Therefore, the extension must not be regionally restricted by the publisher for that location.

Example: nccfelhkfpbnefflolffkclhenplhiab

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM License | Shields.io

**URL:** https://shields.io/badges/npm-license

**Contents:**
- NPM License

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Steam Views | Shields.io

**URL:** https://shields.io/badges/steam-views

**Contents:**
- Steam Views

Using a web browser, you can find the ID in the url here:

In the steam client you can simply just Right-Click and 'Copy Page URL' and follow the above step

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab Tag | Shields.io

**URL:** https://shields.io/badges/git-lab-tag

**Contents:**
- GitLab Tag

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Example: shields-ops-group/tag-test

Example: https://gitlab.com

Possible values: [date, semver]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Dynamic TOML Badge | Shields.io

**URL:** https://shields.io/badges/dynamic-toml-badge

**Contents:**
- Dynamic TOML Badge

The Dynamic TOML Badge allows you to extract an arbitrary value from any TOML Document using a JSONPath selector and show it on a badge.

The URL to a TOML document

Example: https://raw.githubusercontent.com/squirrelchat/smol-toml/mistress/bench/testfiles/toml-spec-example.toml

A JSONPath expression that will be used to query the document

Optional prefix to append to the value

Optional suffix to append to the value

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io MSRV | Shields.io

**URL:** https://shields.io/badges/crates-io-msrv

**Contents:**
- Crates.io MSRV

Crates.io is a package registry for Rust.

MSRV is a crate's minimum suppported rust version, the oldest version of Rust supported by the crate. See the Cargo Book for more info.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PingPong status | Shields.io

**URL:** https://shields.io/badges/ping-pong-status

**Contents:**
- PingPong status

PingPong is a status page and monitoring service.

To see more details about this badge and obtain your api key, visit https://my.pingpong.one/integrations/badge-uptime/

Example: sp_2e80bc00b6054faeb2b87e2464be337e

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Fortify Security Rating (branch) | Shields.io

**URL:** https://shields.io/badges/sonar-fortify-security-rating-branch

**Contents:**
- Sonar Fortify Security Rating (branch)

Note that the Fortify Security Rating badge will only work on Sonar instances that have the Fortify SonarQube Plugin installed. The badge is not available for projects analyzed on SonarCloud.io

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CurseForge Downloads | Shields.io

**URL:** https://shields.io/badges/curse-forge-downloads

**Contents:**
- CurseForge Downloads

The CurseForge badge requires the Project ID in order access the CurseForge API.

The Project ID is different from the URL slug and can be found in the 'About Project' section of your CurseForge mod page.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Chrome Web Store Rating | Shields.io

**URL:** https://shields.io/badges/chrome-web-store-rating

**Contents:**
- Chrome Web Store Rating

Our servers make requests to the Chrome Web Store from the US East region. Therefore, the extension must not be regionally restricted by the publisher for that location.

Example: ogffaloegjglncjfehdfplabnoondfjo

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Modrinth Followers | Shields.io

**URL:** https://shields.io/badges/modrinth-followers

**Contents:**
- Modrinth Followers

You can use your project slug, or the project ID. The ID can be found in the 'Technical information' section of your Modrinth page.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Travis (.com) branch | Shields.io

**URL:** https://shields.io/badges/travis-com-branch

**Contents:**
- Travis (.com) branch

Example: ivandelabeldad

Example: rackian-gateway

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Fedora package (with branch) | Shields.io

**URL:** https://shields.io/badges/fedora-package-with-branch

**Contents:**
- Fedora package (with branch)

See mdapi docs for information on valid branches.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Pub Publisher | Shields.io

**URL:** https://shields.io/badges/pub-publisher

**Contents:**
- Pub Publisher

Pub is a package registry for Dart and Flutter.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Test Count (branch) | Shields.io

**URL:** https://shields.io/badges/sonar-test-count-branch

**Contents:**
- Sonar Test Count (branch)

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Possible values: [total_tests, skipped_tests, test_failures, test_errors]

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub discussions custom search in repo | Shields.io

**URL:** https://shields.io/badges/git-hub-discussions-custom-search-in-repo

**Contents:**
- GitHub discussions custom search in repo

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

For a full list of available filters and allowed values, see GitHub's documentation on Searching discussions.

Example: is:answered answered-by:chris48s

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Quality Gate | Shields.io

**URL:** https://shields.io/badges/sonar-quality-gate

**Contents:**
- Sonar Quality Gate

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab Release | Shields.io

**URL:** https://shields.io/badges/git-lab-release

**Contents:**
- GitLab Release

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Example: gitlab-org/gitlab

Example: https://gitlab.com

Possible values: [date, semver]

Possible values: [tag, release]

Possible values: [created_at, released_at]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab Merge Requests | Shields.io

**URL:** https://shields.io/badges/git-lab-merge-requests

**Contents:**
- GitLab Merge Requests

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

GitLab's API only reports up to 10k Merge Requests, so badges for projects that have more than 10k will not have an exact count.

Possible values: [all, all-raw, open, open-raw, closed, closed-raw, locked, locked-raw, merged, merged-raw]

Example: gitlab-org/gitlab

Example: https://gitlab.com

If you want to use multiple labels, you can use a comma (,) to separate them, e.g. foo,bar

Example: test,type::feature

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub milestone details | Shields.io

**URL:** https://shields.io/badges/git-hub-milestone-details

**Contents:**
- GitHub milestone details

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [issues-closed, issues-open, issues-total, progress, progress-percent]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitlab Code Coverage | Shields.io

**URL:** https://shields.io/badges/gitlab-code-coverage

**Contents:**
- Gitlab Code Coverage

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Important: If your project is publicly visible, but the badge is like this:

Check if your pipelines are publicly visible as well. Navigate to your project settings on GitLab and choose General Pipelines under CI/CD. Then tick the setting Public pipelines.

Now your settings should look like this:

Also make sure you have set up code covrage parsing as described here

Your badge should be working fine now.

Example: gitlab-org/gitlab

Example: https://gitlab.com

Example: jest-integration

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sourcegraph for Repo Reference Count | Shields.io

**URL:** https://shields.io/badges/sourcegraph-for-repo-reference-count

**Contents:**
- Sourcegraph for Repo Reference Count

Sourcegraph for Repo Reference Count

Example: github.com/gorilla/mux

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM Last Update (with dist tag) | Shields.io

**URL:** https://shields.io/badges/npm-last-update-with-dist-tag

**Contents:**
- NPM Last Update (with dist tag)

NPM Last Update (with dist tag)

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Coverage (branch) | Shields.io

**URL:** https://shields.io/badges/sonar-coverage-branch

**Contents:**
- Sonar Coverage (branch)

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: gitify-app_gitify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Conda - License | Shields.io

**URL:** https://shields.io/badges/conda-license

**Contents:**
- Conda - License

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub issue/pull request detail | Shields.io

**URL:** https://shields.io/badges/git-hub-issue-pull-request-detail

**Contents:**
- GitHub issue/pull request detail

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [issues, pulls]

Possible values: [state, title, author, label, comments, age, last-update, milestone]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## F-Droid Version | Shields.io

**URL:** https://shields.io/badges/f-droid-version

**Contents:**
- F-Droid Version

F-Droid is a catalogue of Open Source Android apps.

This badge by default uses f-droid.org, but also supports custom repos.

Example: org.dystopia.email

URL of a third party F-Droid server. If the API is not located at root path, specify the additional path to the API.

Example: https://apt.izzysoft.de/fdroid

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NodePing uptime | Shields.io

**URL:** https://shields.io/badges/node-ping-uptime

**Contents:**
- NodePing uptime

Example: jkiwn052-ntpp-4lbb-8d45-ihew6d9ucoei

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Buildkite (branch) | Shields.io

**URL:** https://shields.io/badges/buildkite-branch

**Contents:**
- Buildkite (branch)

Example: 3826789cf8890b426057e6fe1c4e683bdf04fa24d498885489

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Liberapay receiving | Shields.io

**URL:** https://shields.io/badges/liberapay-receiving

**Contents:**
- Liberapay receiving

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Snapcraft Version | Shields.io

**URL:** https://shields.io/badges/snapcraft-version

**Contents:**
- Snapcraft Version

Architecture, When not specified, this will default to amd64.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Codecov (with branch) | Shields.io

**URL:** https://shields.io/badges/codecov-with-branch

**Contents:**
- Codecov (with branch)

You may specify a Codecov badge token to get coverage for a private repository.

You can find the token under the badge section of your project settings page, in this url: https://codecov.io/[vcsName]/[user]/[repo]/config/badge.

Possible values: [github, gh, bitbucket, bb, gl, gitlab]

Example: example-node

Required only for private repositories.

Display coverage for a specific subset of your project. See Codecov's documentation for details.

Display coverage for a specific component of your project. See Codecov's documentation for details.

Example: component_id_or_name

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Theme Downloads | Shields.io

**URL:** https://shields.io/badges/word-press-theme-downloads

**Contents:**
- WordPress Theme Downloads

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Possible values: [dd, dw, dm, dy, dt]

Daily, Weekly, Monthly, Yearly, or Total downloads

Example: twentyseventeen

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bitrise (branch) | Shields.io

**URL:** https://shields.io/badges/bitrise-branch

**Contents:**
- Bitrise (branch)

Example: 9fa2e96dc9458fbb

Example: abc123def456

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Mozilla Add-on Rating | Shields.io

**URL:** https://shields.io/badges/mozilla-add-on-rating

**Contents:**
- Mozilla Add-on Rating

addons.mozilla.org (AMO) publishes extensions for Mozilla Firefox

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Hangar Downloads | Shields.io

**URL:** https://shields.io/badges/hangar-downloads

**Contents:**
- Hangar Downloads

Hangar is a plugin repository for the Paper, Waterfall and Folia platforms.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub issue custom search | Shields.io

**URL:** https://shields.io/badges/git-hub-issue-custom-search

**Contents:**
- GitHub issue custom search

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

For a full list of available filters and allowed values, see GitHub's documentation on Searching issues and pull requests

Example: repo:badges/shields is:closed label:bug author:app/sentry-io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Open Collective backers | Shields.io

**URL:** https://shields.io/badges/open-collective-backers

**Contents:**
- Open Collective backers

Open Collective backers

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gerrit change status | Shields.io

**URL:** https://shields.io/badges/gerrit-change-status

**Contents:**
- Gerrit change status

Example: https://android-review.googlesource.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM Version | Shields.io

**URL:** https://shields.io/badges/npm-version

**Contents:**
- NPM Version

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Website | Shields.io

**URL:** https://shields.io/badges/website

**Contents:**
- Website

The existence of a specific path on the server can be checked by appending a path after the domain name, e.g. https://img.shields.io/website?url=http%3A//www.website.com/path/to/page.html.

The messages and colors for the up and down states can also be customized.

A site will be classified as "down" if it fails to respond within 3.5 seconds.

Example: https://shields.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub language count | Shields.io

**URL:** https://shields.io/badges/git-hub-language-count

**Contents:**
- GitHub language count

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bitbucket last commit | Shields.io

**URL:** https://shields.io/badges/bitbucket-last-commit

**Contents:**
- Bitbucket last commit

Bitbucket last commit

File path to resolve the last commit for.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Azure DevOps coverage (branch) | Shields.io

**URL:** https://shields.io/badges/azure-dev-ops-coverage-branch

**Contents:**
- Azure DevOps coverage (branch)

Azure Devops (formerly VSO, VSTS) is Microsoft Azure's CI/CD platform.

To obtain your own badge, you need to get 3 pieces of information: ORGANIZATION, PROJECT_ID and DEFINITION_ID.

First, you need to select your build definition and look at the url:

Your badge will then have the form: https://img.shields.io/azure-devops/coverage/ORGANIZATION/PROJECT/DEFINITION_ID.svg.

Optionally, you can specify a named branch: https://img.shields.io/azure-devops/coverage/ORGANIZATION/PROJECT/DEFINITION_ID/NAMED_BRANCH.svg.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub last commit (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-last-commit-branch

**Contents:**
- GitHub last commit (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: infra/config

File path to resolve the last commit for.

Possible values: [author, committer]

Defaults to author if not specified

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Sponsors | Shields.io

**URL:** https://shields.io/badges/git-hub-sponsors

**Contents:**
- GitHub Sponsors

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Liberapay goal progress | Shields.io

**URL:** https://shields.io/badges/liberapay-goal-progress

**Contents:**
- Liberapay goal progress

Liberapay goal progress

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Plugin Downloads | Shields.io

**URL:** https://shields.io/badges/word-press-plugin-downloads

**Contents:**
- WordPress Plugin Downloads

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Possible values: [dd, dw, dm, dy, dt]

Daily, Weekly, Monthly, Yearly, or Total downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Libraries.io dependency status for latest release | Shields.io

**URL:** https://shields.io/badges/libraries-io-dependency-status-for-latest-release

**Contents:**
- Libraries.io dependency status for latest release

Libraries.io dependency status for latest release

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Hex.pm Version | Shields.io

**URL:** https://shields.io/badges/hex-pm-version

**Contents:**
- Hex.pm Version

Hex.pm is a package registry for Erlang

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Steam Subscriptions | Shields.io

**URL:** https://shields.io/badges/steam-subscriptions

**Contents:**
- Steam Subscriptions

Using a web browser, you can find the ID in the url here:

In the steam client you can simply just Right-Click and 'Copy Page URL' and follow the above step

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Pulsar Downloads | Shields.io

**URL:** https://shields.io/badges/pulsar-downloads

**Contents:**
- Pulsar Downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## YouTube Channel Subscribers | Shields.io

**URL:** https://shields.io/badges/you-tube-channel-subscribers

**Contents:**
- YouTube Channel Subscribers

Shields.io is committed to protecting the privacy of its users. We do not collect, store, or track any personal information or data returned by the YouTube API Services. Our service is designed to generate badges based on public YouTube data, and we do not retain any user-specific information:

By using the YouTube badge, you are:

Example: UC8butISFwT-Wl7EV0hUK0BQ

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Docker Stars | Shields.io

**URL:** https://shields.io/badges/docker-stars

**Contents:**
- Docker Stars

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Keybase ZEC | Shields.io

**URL:** https://shields.io/badges/keybase-zec

**Contents:**
- Keybase ZEC

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub contributors from allcontributors.org (with branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-contributors-from-allcontributors-org-with-branch

**Contents:**
- GitHub contributors from allcontributors.org (with branch)

The All Contributors service allows you to recognize all your project contributors, including those that don't push code. See https://allcontributors.org for more information.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: all-contributors

Example: all-contributors

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Repology - Repositories | Shields.io

**URL:** https://shields.io/badges/repology-repositories

**Contents:**
- Repology - Repositories

Repology - Repositories

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bluesky followers | Shields.io

**URL:** https://shields.io/badges/bluesky-followers

**Contents:**
- Bluesky followers

Displays the number of Bluesky followers for a given handle using the public Bluesky API.

Bluesky handle (user ID)

Example: chitvs.bsky.social

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SymfonyInsight Stars | Shields.io

**URL:** https://shields.io/badges/symfony-insight-stars

**Contents:**
- SymfonyInsight Stars

SymfonyInsight (formerly SensioLabs) is a code analysis service

Example: 825be328-29f8-44f7-a750-f82818ae9111

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## JetBrains Plugin Version | Shields.io

**URL:** https://shields.io/badges/jet-brains-plugin-version

**Contents:**
- JetBrains Plugin Version

JetBrains Plugin Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Static Badges | Shields.io

**URL:** https://shields.io/docs/static-badges

**Contents:**
- Static Badges

It is possible to use shields.io to make a wide variety of badges displaying static text and/or logos. For example:

---

## Chromium HSTS preload | Shields.io

**URL:** https://shields.io/badges/chromium-hsts-preload

**Contents:**
- Chromium HSTS preload

Strict-Transport-Security is an HTTP response header that signals that browsers should only access the site using HTTPS.

For a higher level of security, it's possible for a domain owner to preload this behavior into participating web browsers. Chromium maintains the HSTS preload list, which is the de facto standard that has been adopted by several browsers. This service checks a domain's status in that list.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer coverage (GitHub/Bitbucket) | Shields.io

**URL:** https://shields.io/badges/scrutinizer-coverage-git-hub-bitbucket

**Contents:**
- Scrutinizer coverage (GitHub/Bitbucket)

Scrutinizer coverage (GitHub/Bitbucket)

Possible values: [g, b]

Platform: Either GitHub or Bitbucket

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub commit check runs | Shields.io

**URL:** https://shields.io/badges/git-hub-commit-check-runs

**Contents:**
- GitHub commit check runs

The Check Runs service shows the status of GitHub action runs.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: 91b108d4b7359b2f8794a4614c11cb1157dc9fff

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Codacy coverage | Shields.io

**URL:** https://shields.io/badges/codacy-coverage

**Contents:**
- Codacy coverage

Example: 84c0a068ce9349f2bcaa07b5977bd932

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM dev or peer Dependency Version | Shields.io

**URL:** https://shields.io/badges/npm-dev-or-peer-dependency-version

**Contents:**
- NPM dev or peer Dependency Version

NPM dev or peer Dependency Version

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: react-boxplot

Possible values: [dev, peer]

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM Downloads by package author | Shields.io

**URL:** https://shields.io/badges/npm-downloads-by-package-author

**Contents:**
- NPM Downloads by package author

The total number of downloads of npm packages published by the specified author from npm-stat.

Possible values: [dw, dm, dy]

Downloads per Week, Month or Year

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab Top Language | Shields.io

**URL:** https://shields.io/badges/git-lab-top-language

**Contents:**
- GitLab Top Language

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Example: gitlab-org/gitlab

Example: https://gitlab.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Cocoapods doc percentage | Shields.io

**URL:** https://shields.io/badges/cocoapods-doc-percentage

**Contents:**
- Cocoapods doc percentage

Cocoapods doc percentage

Example: AFNetworking

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Codacy grade (branch) | Shields.io

**URL:** https://shields.io/badges/codacy-grade-branch

**Contents:**
- Codacy grade (branch)

Codacy grade (branch)

Example: 0cb32ce695b743d68257021455330c66

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Tag | Shields.io

**URL:** https://shields.io/badges/git-hub-tag

**Contents:**
- GitHub Tag

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [date, semver]

The filter param can be used to apply a filter to the project's tag or release names before selecting the latest from the list. Two constructs are available: * is a wildcard matching zero or more characters, and if the pattern starts with a !, the whole pattern is negated.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## npm bundle size (scoped version) | Shields.io

**URL:** https://shields.io/badges/npm-bundle-size-scoped-version

**Contents:**
- npm bundle size (scoped version)

Bundlephobia lets you understand the size of a javascript package from NPM before it becomes a part of your bundle.

Possible values: [min, minzip]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Weblate component license | Shields.io

**URL:** https://shields.io/badges/weblate-component-license

**Contents:**
- Weblate component license

Weblate is an web-based tool for translation and internationalization

Example: godot-engine

Example: https://hosted.weblate.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## X (formerly Twitter) Follow | Shields.io

**URL:** https://shields.io/badges/x-formerly-twitter-follow

**Contents:**
- X (formerly Twitter) Follow

X (formerly Twitter) Follow

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonatype Nexus (Snapshots) | Shields.io

**URL:** https://shields.io/badges/sonatype-nexus-snapshots

**Contents:**
- Sonatype Nexus (Snapshots)

Sonatype Nexus (Snapshots)

Example: com.google.guava

Example: https://repo.tomkeuper.com

Note that you can use query options with any Nexus badge type (Releases, Snapshots, or Repository).

Query options should be provided as key=value pairs separated by a colon.

Possible values: Searching for Component

Example: :c=agent-apple-osx:p=tar.gz

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Maintenance | Shields.io

**URL:** https://shields.io/badges/maintenance

**Contents:**
- Maintenance

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitea language count | Shields.io

**URL:** https://shields.io/badges/gitea-language-count

**Contents:**
- Gitea language count

By default this badge looks for repositories on gitea.com. To specify another instance like codeberg, forgejo or a self-hosted instance, use the gitea_url query param.

Example: https://gitea.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer quality (GitLab) with branch | Shields.io

**URL:** https://shields.io/badges/scrutinizer-quality-git-lab-with-branch

**Contents:**
- Scrutinizer quality (GitLab) with branch

Scrutinizer quality (GitLab) with branch

Example: propertywindow

Example: propertywindow

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io Dependents | Shields.io

**URL:** https://shields.io/badges/crates-io-dependents

**Contents:**
- Crates.io Dependents

Crates.io is a package registry for Rust.

Due to upstream API timeouts, crates with a very large number of dependents may fail to retrieve data.

See #11633 for more info.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Theme Last Updated | Shields.io

**URL:** https://shields.io/badges/word-press-theme-last-updated

**Contents:**
- WordPress Theme Last Updated

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Example: twentyseventeen

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Theme Rating | Shields.io

**URL:** https://shields.io/badges/word-press-theme-rating

**Contents:**
- WordPress Theme Rating

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Example: twentyseventeen

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Cdnjs | Shields.io

**URL:** https://shields.io/badges/cdnjs

**Contents:**
- Cdnjs

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## YouTube Video Views | Shields.io

**URL:** https://shields.io/badges/you-tube-video-views

**Contents:**
- YouTube Video Views

Shields.io is committed to protecting the privacy of its users. We do not collect, store, or track any personal information or data returned by the YouTube API Services. Our service is designed to generate badges based on public YouTube data, and we do not retain any user-specific information:

By using the YouTube badge, you are:

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub pull request status | Shields.io

**URL:** https://shields.io/badges/git-hub-pull-request-status

**Contents:**
- GitHub pull request status

Displays the status of a pull request, as reported by the Commit Status API.

Note: Nowadays, GitHub Actions and many third party integrations report state via the Checks API. If this badge does not show expected values, please try out one of our Check Runs badges instead. You can read more about status checks in the GitHub documentation.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SourceForge Downloads | Shields.io

**URL:** https://shields.io/badges/source-forge-downloads

**Contents:**
- SourceForge Downloads

SourceForge Downloads

Possible values: [dd, dw, dm, dt]

Daily, Weekly, Monthly, or Total downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Chrome Web Store Size | Shields.io

**URL:** https://shields.io/badges/chrome-web-store-size

**Contents:**
- Chrome Web Store Size

Our servers make requests to the Chrome Web Store from the US East region. Therefore, the extension must not be regionally restricted by the publisher for that location.

Example: nccfelhkfpbnefflolffkclhenplhiab

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AUR Maintainer | Shields.io

**URL:** https://shields.io/badges/aur-maintainer

**Contents:**
- AUR Maintainer

Arch linux User Repository Maintainer

Example: google-chrome

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Chocolatey Downloads | Shields.io

**URL:** https://shields.io/badges/chocolatey-downloads

**Contents:**
- Chocolatey Downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Violations | Shields.io

**URL:** https://shields.io/badges/sonar-violations

**Contents:**
- Sonar Violations

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Possible values: [violations, blocker_violations, critical_violations, major_violations, minor_violations, info_violations]

Example: brave_brave-core

Example: https://sonarcloud.io

Possible values: [short, long]

If not specified, the default is short.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Liberapay giving | Shields.io

**URL:** https://shields.io/badges/liberapay-giving

**Contents:**
- Liberapay giving

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Liberapay patrons | Shields.io

**URL:** https://shields.io/badges/liberapay-patrons

**Contents:**
- Liberapay patrons

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab Contributors | Shields.io

**URL:** https://shields.io/badges/git-lab-contributors

**Contents:**
- GitLab Contributors

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Example: gitlab-org/gitlab

Example: https://gitlab.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab Stars | Shields.io

**URL:** https://shields.io/badges/git-lab-stars

**Contents:**
- GitLab Stars

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Example: gitlab-org/gitlab

Example: https://gitlab.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bitbucket open pull requests (raw) | Shields.io

**URL:** https://shields.io/badges/bitbucket-open-pull-requests-raw

**Contents:**
- Bitbucket open pull requests (raw)

Bitbucket open pull requests (raw)

When not specified, this will default to https://bitbucket.org.

Example: https://bitbucket.mydomain.net

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gem Owner | Shields.io

**URL:** https://shields.io/badges/gem-owner

**Contents:**
- Gem Owner

Ruby Gems is a registry for ruby libraries

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gem Downloads (for latest version) | Shields.io

**URL:** https://shields.io/badges/gem-downloads-for-latest-version

**Contents:**
- Gem Downloads (for latest version)

Ruby Gems is a registry for ruby libraries

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Docker Image Size | Shields.io

**URL:** https://shields.io/badges/docker-image-size

**Contents:**
- Docker Image Size

Possible values: [date, semver]

If not specified, the default is date

Possible values: [amd64, arm, arm64, s390x, 386, ppc64, ppc64le, wasm, mips, mipsle, mips64, mips64le, riscv64, loong64]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CurseForge Game Versions | Shields.io

**URL:** https://shields.io/badges/curse-forge-game-versions

**Contents:**
- CurseForge Game Versions

The CurseForge badge requires the Project ID in order access the CurseForge API.

The Project ID is different from the URL slug and can be found in the 'About Project' section of your CurseForge mod page.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Cirrus CI - Specific Branch Build Status | Shields.io

**URL:** https://shields.io/badges/cirrus-ci-specific-branch-build-status

**Contents:**
- Cirrus CI - Specific Branch Build Status

Cirrus CI - Specific Branch Build Status

Example: build_docker

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io Downloads (latest version) | Shields.io

**URL:** https://shields.io/badges/crates-io-downloads-latest-version

**Contents:**
- Crates.io Downloads (latest version)

Crates.io is a package registry for Rust.

Example: rustc-serialize

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## JIRA sprint completion | Shields.io

**URL:** https://shields.io/badges/jira-sprint-completion

**Contents:**
- JIRA sprint completion

To get the Sprint ID, go to your Backlog view in your project, right click on your sprint name and get the value of data-sprint-id.

Example: https://issues.apache.org/jira

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Hangar Watchers | Shields.io

**URL:** https://shields.io/badges/hangar-watchers

**Contents:**
- Hangar Watchers

Hangar is a plugin repository for the Paper, Waterfall and Folia platforms.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Keybase XLM | Shields.io

**URL:** https://shields.io/badges/keybase-xlm

**Contents:**
- Keybase XLM

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Twitch Extension Version | Shields.io

**URL:** https://shields.io/badges/twitch-extension-version

**Contents:**
- Twitch Extension Version

Twitch Extension Version

Example: 2nq5cu1nc9f4p75b791w8d3yo9d195

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SourceForge Languages | Shields.io

**URL:** https://shields.io/badges/source-forge-languages

**Contents:**
- SourceForge Languages

SourceForge Languages

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub User's stars | Shields.io

**URL:** https://shields.io/badges/git-hub-users-stars

**Contents:**
- GitHub User's stars

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Note: This badge takes into account up to 200 of the most starred repositories of given user / org.

This param accepts three values (must be UPPER case) OWNER, COLLABORATOR, ORGANIZATION_MEMBER. One can pass comma separated combinations of these values (no spaces) e.g. OWNER,COLLABORATOR or OWNER,COLLABORATOR,ORGANIZATION_MEMBER. Default value is OWNER. See the explanation of these values here.

Example: OWNER,COLLABORATOR

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Galaxy Toolshed - Tool Requirement Version | Shields.io

**URL:** https://shields.io/badges/galaxy-toolshed-tool-requirement-version

**Contents:**
- Galaxy Toolshed - Tool Requirement Version

Galaxy Toolshed - Tool Requirement Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## ROS Package Index | Shields.io

**URL:** https://shields.io/badges/ros-package-index

**Contents:**
- ROS Package Index

To use this badge, specify the ROS distribution (e.g. noetic or humble) and the package repository name (in the case of single-package repos, this may be the same as the package name). This badge determines which versions are part of an official ROS distribution by fetching from the rosdistro YAML files, at the tag corresponding to the latest release.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PowerShell Gallery Downloads | Shields.io

**URL:** https://shields.io/badges/power-shell-gallery-downloads

**Contents:**
- PowerShell Gallery Downloads

PowerShell Gallery Downloads

Example: Azure.Storage

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Created At | Shields.io

**URL:** https://shields.io/badges/git-hub-created-at

**Contents:**
- GitHub Created At

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bitrise | Shields.io

**URL:** https://shields.io/badges/bitrise

**Contents:**
- Bitrise

Example: 9fa2e96dc9458fbb

Example: abc123def456

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## JetBrains Plugin Downloads | Shields.io

**URL:** https://shields.io/badges/jet-brains-plugin-downloads

**Contents:**
- JetBrains Plugin Downloads

JetBrains Plugin Downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AppVeyor Job (with branch) | Shields.io

**URL:** https://shields.io/badges/app-veyor-job-with-branch

**Contents:**
- AppVeyor Job (with branch)

AppVeyor Job (with branch)

Example: wpmgprostotema

Example: voicetranscoder

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Repo stars | Shields.io

**URL:** https://shields.io/badges/git-hub-repo-stars

**Contents:**
- GitHub Repo stars

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Azure DevOps tests (branch) | Shields.io

**URL:** https://shields.io/badges/azure-dev-ops-tests-branch

**Contents:**
- Azure DevOps tests (branch)

Azure Devops (formerly VSO, VSTS) is Microsoft Azure's CI/CD platform.

To obtain your own badge, you need to get 3 pieces of information: ORGANIZATION, PROJECT, DEFINITION_ID.

First, you need to select your build definition and look at the url:

Your badge will then have the form: https://img.shields.io/azure-devops/tests/ORGANIZATION/PROJECT/DEFINITION_ID.svg.

Optionally, you can specify a named branch: https://img.shields.io/azure-devops/tests/ORGANIZATION/PROJECT/DEFINITION_ID/NAMED_BRANCH.svg.

You may change the "passed", "failed" and "skipped" text on this badge by supplying query parameters &passed_label=, &failed_label= and &skipped_label= respectively.

For example, if you want to use a different terminology:

?passed_label=good&failed_label=bad&skipped_label=n%2Fa

?compact_message&passed_label=💃&failed_label=🤦‍♀️&skipped_label=🤷

There is also a &compact_message query parameter, which will default to displaying ✔, ✘ and ➟, separated by a horizontal bar |.

Example: azuredevops-powershell

Example: azuredevops-powershell

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CodeRabbit Pull Request Reviews | Shields.io

**URL:** https://shields.io/badges/code-rabbit-pull-request-reviews

**Contents:**
- CodeRabbit Pull Request Reviews

This badge pulls the number of PRs reviewed by CodeRabbit, AI code review tool

Possible values: [github, bitbucket, gitlab]

Version Control Provider

Organization or User name

Example: coderabbitai

Example: ast-grep-essentials

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Clojars Version | Shields.io

**URL:** https://shields.io/badges/clojars-version

**Contents:**
- Clojars Version

Clojars is a repository for Clojure libraries

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PyPI - Version | Shields.io

**URL:** https://shields.io/badges/py-pi-version

**Contents:**
- PyPI - Version

Example: https://pypi.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Theme Required PHP Version | Shields.io

**URL:** https://shields.io/badges/word-press-theme-required-php-version

**Contents:**
- WordPress Theme Required PHP Version

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Example: twentytwenty

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io Size | Shields.io

**URL:** https://shields.io/badges/crates-io-size

**Contents:**
- Crates.io Size

Crates.io is a package registry for Rust.

Example: rustc-serialize

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub package.json prod dependency version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-package-json-prod-dependency-version-branch

**Contents:**
- GitHub package.json prod dependency version (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: badge-maker/package.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Greasy Fork License | Shields.io

**URL:** https://shields.io/badges/greasy-fork-license

**Contents:**
- Greasy Fork License

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Theme Version | Shields.io

**URL:** https://shields.io/badges/word-press-theme-version

**Contents:**
- WordPress Theme Version

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Example: twentyseventeen

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Modrinth Downloads | Shields.io

**URL:** https://shields.io/badges/modrinth-downloads

**Contents:**
- Modrinth Downloads

You can use your project slug, or the project ID. The ID can be found in the 'Technical information' section of your Modrinth page.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## DUB Version | Shields.io

**URL:** https://shields.io/badges/dub-version

**Contents:**
- DUB Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Clojars Downloads | Shields.io

**URL:** https://shields.io/badges/clojars-downloads

**Contents:**
- Clojars Downloads

Clojars is a repository for Clojure libraries

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub labels | Shields.io

**URL:** https://shields.io/badges/git-hub-labels

**Contents:**
- GitHub labels

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## W3C Validation | Shields.io

**URL:** https://shields.io/badges/w-3-c-validation

**Contents:**
- W3C Validation

The W3C validation badge performs validation of the HTML, SVG, MathML, ITS, RDFa Lite, XHTML documents. The badge uses the type property of each message found in the messages from the validation results to determine to be an error or warning. The rules are as follows:

This badge relies on the https://validator.nu/ service to perform the validation. Please refer to https://about.validator.nu/ for the full documentation and Terms of service.

Possible values: [default, html, xml, xmldtd]

The parser that is used for validation. This is a passthru value to the service

URL of the document to be validate

Example: https://validator.nu/

Possible values: [HTML, SVG 1.1, MathML 3.0, HTML, SVG 1.1, MathML 3.0, ITS 2.0, HTML, SVG 1.1, MathML 3.0, RDFa Lite 1.1, HTML 4.01 Strict, URL / XHTML 1.0 Strict, URL, HTML 4.01 Transitional, URL / XHTML 1.0 Transitional, URL, HTML 4.01 Frameset, URL / XHTML 1.0 Frameset, URL, XHTML, SVG 1.1, MathML 3.0, XHTML, SVG 1.1, MathML 3.0, RDFa Lite 1.1, XHTML 1.0 Strict, URL, Ruby, SVG 1.1, MathML 3.0, SVG 1.1, URL, XHTML, MathML 3.0]

This is used to determine the schema for the document to be valdiated against.

Example: HTML, SVG 1.1, MathML 3.0

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SourceForge Contributors | Shields.io

**URL:** https://shields.io/badges/source-forge-contributors

**Contents:**
- SourceForge Contributors

SourceForge Contributors

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CircleCI | Shields.io

**URL:** https://shields.io/badges/circle-ci

**Contents:**
- CircleCI

Possible values: [github, gh, bitbucket, bb]

Example: node-csgo-parser

You may specify an optional token to get the status for a private repository.

If you need to use a token, please use a Project Token and only assign your token the 'Status' permission. Never use a Personal Token as they grant full read write permissions to your projects.

For more information about managing Circle CI tokens, please read this article.

Example: abc123def456

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Coverage | Shields.io

**URL:** https://shields.io/badges/sonar-coverage

**Contents:**
- Sonar Coverage

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: gitify-app_gitify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Steam Downloads | Shields.io

**URL:** https://shields.io/badges/steam-downloads

**Contents:**
- Steam Downloads

Using a web browser, you can find the ID in the url here:

In the steam client you can simply just Right-Click and 'Copy Page URL' and follow the above step

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Discourse Users | Shields.io

**URL:** https://shields.io/badges/discourse-users

**Contents:**
- Discourse Users

Example: https://meta.discourse.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub package.json dev/peer/optional dependency version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-package-json-dev-peer-optional-dependency-version-branch

**Contents:**
- GitHub package.json dev/peer/optional dependency version (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [dev, peer, optional]

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: packages/gatsby-cli/package.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ore Downloads | Shields.io

**URL:** https://shields.io/badges/ore-downloads

**Contents:**
- Ore Downloads

Ore is a Minecraft package repository.

Your Plugin ID is the name of your plugin in lowercase, without any spaces or dashes.

Example: https://ore.spongepowered.org/Erigitic/Total-Economy - Here the Plugin ID is totaleconomy.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scoop License | Shields.io

**URL:** https://shields.io/badges/scoop-license

**Contents:**
- Scoop License

Scoop is a command-line installer for Windows

App's containing bucket. Can either be a name (e.g extras) or a URL to a GitHub Repo (e.g https://github.com/jewlexx/personal-scoop)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Steam Release Date | Shields.io

**URL:** https://shields.io/badges/steam-release-date

**Contents:**
- Steam Release Date

Using a web browser, you can find the ID in the url here:

In the steam client you can simply just Right-Click and 'Copy Page URL' and follow the above step

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Downloads (specific asset, all releases) | Shields.io

**URL:** https://shields.io/badges/git-hub-downloads-specific-asset-all-releases

**Contents:**
- GitHub Downloads (specific asset, all releases)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: atom-amd64.deb

Whether to display the asset name in the badge value

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer quality (GitLab) | Shields.io

**URL:** https://shields.io/badges/scrutinizer-quality-git-lab

**Contents:**
- Scrutinizer quality (GitLab)

Scrutinizer quality (GitLab)

Example: propertywindow

Example: propertywindow

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitter | Shields.io

**URL:** https://shields.io/badges/gitter

**Contents:**
- Gitter

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Thunderstore Likes | Shields.io

**URL:** https://shields.io/badges/thunderstore-likes

**Contents:**
- Thunderstore Likes

The Thunderstore badges require a package's namespace and name.

Everything can be discerned from your package's URL. Thunderstore package URLs have a mostly consistent format:

For example: https://thunderstore.io/c/lethal-company/p/notnotnotswipez/MoreCompany/.

The 'default community', Risk of Rain 2, has an alternative URL:

Some communities use a 'subdomain' alternative URL, for example, Valheim:

Example: notnotnotswipez

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab Forks | Shields.io

**URL:** https://shields.io/badges/git-lab-forks

**Contents:**
- GitLab Forks

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Example: gitlab-org/gitlab

Example: https://gitlab.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Theme Stars | Shields.io

**URL:** https://shields.io/badges/word-press-theme-stars

**Contents:**
- WordPress Theme Stars

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Example: twentyseventeen

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io MSRV (version) | Shields.io

**URL:** https://shields.io/badges/crates-io-msrv-version

**Contents:**
- Crates.io MSRV (version)

Crates.io is a package registry for Rust.

MSRV is a crate's minimum suppported rust version, the oldest version of Rust supported by the crate. See the Cargo Book for more info.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitea Last Commit (branch) | Shields.io

**URL:** https://shields.io/badges/gitea-last-commit-branch

**Contents:**
- Gitea Last Commit (branch)

By default this badge looks for repositories on gitea.com. To specify another instance like codeberg, forgejo or a self-hosted instance, use the gitea_url query param.

File path to resolve the last commit for.

Possible values: [author, committer]

Defaults to author if not specified

Example: https://gitea.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Node LTS | Shields.io

**URL:** https://shields.io/badges/node-lts

**Contents:**
- Node LTS

This badge indicates whether the package supports all LTS node versions.

The node version support is retrieved from the engines.node section in package.json.

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Violations (branch) | Shields.io

**URL:** https://shields.io/badges/sonar-violations-branch

**Contents:**
- Sonar Violations (branch)

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Possible values: [violations, blocker_violations, critical_violations, major_violations, minor_violations, info_violations]

Example: :brave_brave-core

Example: https://sonarcloud.io

Possible values: [short, long]

If not specified, the default is short.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Discourse Status | Shields.io

**URL:** https://shields.io/badges/discourse-status

**Contents:**
- Discourse Status

Example: https://meta.discourse.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Docker Image Version (tag) | Shields.io

**URL:** https://shields.io/badges/docker-image-version-tag

**Contents:**
- Docker Image Version (tag)

Docker Image Version (tag)

Possible values: [date, semver]

If not specified, the default is date

Possible values: [amd64, arm, arm64, s390x, 386, ppc64, ppc64le, wasm, mips, mipsle, mips64, mips64le, riscv64, loong64]

If not specified, the default is amd64

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Pepy Total Downloads | Shields.io

**URL:** https://shields.io/badges/pepy-total-downloads

**Contents:**
- Pepy Total Downloads

Python package total downloads from Pepy.

Download stats from pepy count package downloads from PyPI and known mirrors.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Testspace pass ratio | Shields.io

**URL:** https://shields.io/badges/testspace-pass-ratio

**Contents:**
- Testspace pass ratio

Example: swellaby:testspace-sample

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io Downloads (recent) | Shields.io

**URL:** https://shields.io/badges/crates-io-downloads-recent

**Contents:**
- Crates.io Downloads (recent)

Crates.io is a package registry for Rust.

Example: rustc-serialize

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AppVeyor tests | Shields.io

**URL:** https://shields.io/badges/app-veyor-tests

**Contents:**
- AppVeyor tests

You may change the "passed", "failed" and "skipped" text on this badge by supplying query parameters &passed_label=, &failed_label= and &skipped_label= respectively.

For example, if you want to use a different terminology:

?passed_label=good&failed_label=bad&skipped_label=n%2Fa

?compact_message&passed_label=💃&failed_label=🤦‍♀️&skipped_label=🤷

There is also a &compact_message query parameter, which will default to displaying ✔, ✘ and ➟, separated by a horizontal bar |.

Example: coap-net-iu0to

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub manifest version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-manifest-version-branch

**Contents:**
- GitHub manifest version (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: sindresorhus

Example: show-all-github-issues

Example: extension/manifest.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Localizely progress (branch) | Shields.io

**URL:** https://shields.io/badges/localizely-progress-branch

**Contents:**
- Localizely progress (branch)

Localizely is a management system for translation, localization, and internationalization of your projects.

The read-only API token from the Localizely account is required to fetch necessary data.

Note: Do not use the default API token as it grants full read-write permissions to your projects. You will expose your project and allow malicious users to modify the translations at will. Instead, create a new one with only read permission.

You can find more details regarding API tokens under My profile page.

Example: 5cc34208-0418-40b1-8353-acc70c95f802

Example: 0f4d5e31a44f48dcbab966c52cfb0a67c5f1982186c14b85ab389a031dbc225a

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## DUB Score | Shields.io

**URL:** https://shields.io/badges/dub-score

**Contents:**
- DUB Score

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## npm package minimized gzipped size (scoped) | Shields.io

**URL:** https://shields.io/badges/npm-package-minimized-gzipped-size-scoped

**Contents:**
- npm package minimized gzipped size (scoped)

View esbuild minified and denoflate gzipped size of a javascript package or selected exports, via bundlejs.

This can either be a package name e.g: falso, or a package name and version e.g: falso@6.4.0

Example: randEmail,randFullName

Example: lodash,axios

Possible values: [min, minzip, both]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab License | Shields.io

**URL:** https://shields.io/badges/git-lab-license

**Contents:**
- GitLab License

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Example: gitlab-org/gitlab

Example: https://gitlab.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AUR Popularity | Shields.io

**URL:** https://shields.io/badges/aur-popularity

**Contents:**
- AUR Popularity

Arch linux User Repository Popularity

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Uptime Robot status | Shields.io

**URL:** https://shields.io/badges/uptime-robot-status

**Contents:**
- Uptime Robot status

Example: m778918918-3e92c097147760ee39d02d36

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Open VSX Release Date | Shields.io

**URL:** https://shields.io/badges/open-vsx-release-date

**Contents:**
- Open VSX Release Date

Open VSX (OVSX) is a registry of extensions for VS Code compatible editors.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Plugin: Required WP Version | Shields.io

**URL:** https://shields.io/badges/word-press-plugin-required-wp-version

**Contents:**
- WordPress Plugin: Required WP Version

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PowerShell Gallery Platform Support | Shields.io

**URL:** https://shields.io/badges/power-shell-gallery-platform-support

**Contents:**
- PowerShell Gallery Platform Support

PowerShell Gallery Platform Support

Example: PackageManagement

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Discourse Topics | Shields.io

**URL:** https://shields.io/badges/discourse-topics

**Contents:**
- Discourse Topics

Example: https://meta.discourse.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Buildkite | Shields.io

**URL:** https://shields.io/badges/buildkite

**Contents:**
- Buildkite

Example: 3826789cf8890b426057e6fe1c4e683bdf04fa24d498885489

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub last commit | Shields.io

**URL:** https://shields.io/badges/git-hub-last-commit

**Contents:**
- GitHub last commit

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

File path to resolve the last commit for.

Possible values: [author, committer]

Defaults to author if not specified

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Mozilla Add-on Version | Shields.io

**URL:** https://shields.io/badges/mozilla-add-on-version

**Contents:**
- Mozilla Add-on Version

addons.mozilla.org (AMO) publishes extensions for Mozilla Firefox

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## jsDelivr hits (npm scoped) | Shields.io

**URL:** https://shields.io/badges/js-delivr-hits-npm-scoped

**Contents:**
- jsDelivr hits (npm scoped)

jsDelivr hits (npm scoped)

Possible values: [hd, hw, hm, hy]

Hits per Day, Week, Month or Year

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Conda Downloads | Shields.io

**URL:** https://shields.io/badges/conda-downloads

**Contents:**
- Conda Downloads

Possible values: [d, dn]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## jsDelivr hits (npm) | Shields.io

**URL:** https://shields.io/badges/js-delivr-hits-npm

**Contents:**
- jsDelivr hits (npm)

Possible values: [hd, hw, hm, hy]

Hits per Day, Week, Month or Year

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AppVeyor Build (with branch) | Shields.io

**URL:** https://shields.io/badges/app-veyor-build-with-branch

**Contents:**
- AppVeyor Build (with branch)

AppVeyor Build (with branch)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Codecov | Shields.io

**URL:** https://shields.io/badges/codecov

**Contents:**
- Codecov

You may specify a Codecov badge token to get coverage for a private repository.

You can find the token under the badge section of your project settings page, in this url: https://codecov.io/[vcsName]/[user]/[repo]/config/badge.

Possible values: [github, gh, bitbucket, bb, gl, gitlab]

Example: example-node

Required only for private repositories.

Display coverage for a specific subset of your project. See Codecov's documentation for details.

Display coverage for a specific component of your project. See Codecov's documentation for details.

Example: component_id_or_name

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub contributors from allcontributors.org | Shields.io

**URL:** https://shields.io/badges/git-hub-contributors-from-allcontributors-org

**Contents:**
- GitHub contributors from allcontributors.org

The All Contributors service allows you to recognize all your project contributors, including those that don't push code. See https://allcontributors.org for more information.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: all-contributors

Example: all-contributors

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Stack Exchange monthly questions | Shields.io

**URL:** https://shields.io/badges/stack-exchange-monthly-questions

**Contents:**
- Stack Exchange monthly questions

Stack Exchange monthly questions

Example: stackoverflow

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## POEditor | Shields.io

**URL:** https://shields.io/badges/po-editor

**Contents:**
- POEditor

POEditor is an web-based tool for translation and internationalization

All requests to must contain the parameter token. You can get a read-only token from your POEditor account in My Account > API Access. This token will be exposed as part of the badge URL so be sure to generate a read-only token.

A read-only token from your POEditor account from My Account > API Access

Example: abc123def456

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Tests (branch) | Shields.io

**URL:** https://shields.io/badges/sonar-tests-branch

**Contents:**
- Sonar Tests (branch)

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

You may change the "passed", "failed" and "skipped" text on this badge by supplying query parameters &passed_label=, &failed_label= and &skipped_label= respectively.

For example, if you want to use a different terminology:

?passed_label=good&failed_label=bad&skipped_label=n%2Fa

?compact_message&passed_label=💃&failed_label=🤦‍♀️&skipped_label=🤷

There is also a &compact_message query parameter, which will default to displaying ✔, ✘ and ➟, separated by a horizontal bar |.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SourceForge Commit Count | Shields.io

**URL:** https://shields.io/badges/source-forge-commit-count

**Contents:**
- SourceForge Commit Count

SourceForge Commit Count

The repository name, usually git but might be different.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gem Version | Shields.io

**URL:** https://shields.io/badges/gem-version

**Contents:**
- Gem Version

Ruby Gems is a registry for ruby libraries

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Chef cookbook | Shields.io

**URL:** https://shields.io/badges/chef-cookbook

**Contents:**
- Chef cookbook

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Vaadin Directory Status | Shields.io

**URL:** https://shields.io/badges/vaadin-directory-status

**Contents:**
- Vaadin Directory Status

Vaadin Directory Status

Example: vaadinvaadin-grid

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## bStats Players | Shields.io

**URL:** https://shields.io/badges/b-stats-players

**Contents:**
- bStats Players

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Coveralls | Shields.io

**URL:** https://shields.io/badges/coveralls

**Contents:**
- Coveralls

Possible values: [github, bitbucket, gitlab]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bitbucket open issues (raw) | Shields.io

**URL:** https://shields.io/badges/bitbucket-open-issues-raw

**Contents:**
- Bitbucket open issues (raw)

Bitbucket open issues (raw)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Maven Central Last Update | Shields.io

**URL:** https://shields.io/badges/maven-central-last-update

**Contents:**
- Maven Central Last Update

Maven Central Last Update

Example: com.google.guava

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Hackage Version | Shields.io

**URL:** https://shields.io/badges/hackage-version

**Contents:**
- Hackage Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## UptimeObserver uptime ratio (1 day) | Shields.io

**URL:** https://shields.io/badges/uptime-observer-uptime-ratio-1-day

**Contents:**
- UptimeObserver uptime ratio (1 day)

UptimeObserver uptime ratio (1 day)

Example: 33Zw1rnH6veb4OLcskqvj6g9Lj4tnyxZ41

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PingPong uptime (last 30 days) | Shields.io

**URL:** https://shields.io/badges/ping-pong-uptime-last-30-days

**Contents:**
- PingPong uptime (last 30 days)

PingPong is a status page and monitoring service.

To see more details about this badge and obtain your api key, visit https://my.pingpong.one/integrations/badge-uptime/

Example: sp_2e80bc00b6054faeb2b87e2464be337e

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Mozilla HTTP Observatory Grade | Shields.io

**URL:** https://shields.io/badges/mozilla-http-observatory-grade

**Contents:**
- Mozilla HTTP Observatory Grade

The Mozilla HTTP Observatory is a set of security tools to analyze your website and inform you if you are utilizing the many available methods to secure it.

Possible values: [grade, grade-score]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub discussions custom search | Shields.io

**URL:** https://shields.io/badges/git-hub-discussions-custom-search

**Contents:**
- GitHub discussions custom search

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

For a full list of available filters and allowed values, see GitHub's documentation on Searching discussions.

Example: repo:badges/shields is:answered answered-by:chris48s

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM Unpacked Size | Shields.io

**URL:** https://shields.io/badges/npm-unpacked-size

**Contents:**
- NPM Unpacked Size

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Test Success Rate (branch) | Shields.io

**URL:** https://shields.io/badges/sonar-test-success-rate-branch

**Contents:**
- Sonar Test Success Rate (branch)

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Stack Exchange questions | Shields.io

**URL:** https://shields.io/badges/stack-exchange-questions

**Contents:**
- Stack Exchange questions

Stack Exchange questions

Example: stackoverflow

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ore Version | Shields.io

**URL:** https://shields.io/badges/ore-version

**Contents:**
- Ore Version

Ore is a Minecraft package repository.

Your Plugin ID is the name of your plugin in lowercase, without any spaces or dashes.

Example: https://ore.spongepowered.org/Erigitic/Total-Economy - Here the Plugin ID is totaleconomy.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Spiget Download Size | Shields.io

**URL:** https://shields.io/badges/spiget-download-size

**Contents:**
- Spiget Download Size

Spiget holds information about SpigotMC Resources, Plugins and Authors.

You can find your resource ID in the url for your resource page.

Example: https://www.spigotmc.org/resources/essentialsx.9089/ - Here the Resource ID is 9089.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Chrome Web Store Rating Count | Shields.io

**URL:** https://shields.io/badges/chrome-web-store-rating-count

**Contents:**
- Chrome Web Store Rating Count

Our servers make requests to the Chrome Web Store from the US East region. Therefore, the extension must not be regionally restricted by the publisher for that location.

Example: ogffaloegjglncjfehdfplabnoondfjo

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Reproducible Central Artifact | Shields.io

**URL:** https://shields.io/badges/reproducible-central-artifact

**Contents:**
- Reproducible Central Artifact

Reproducible Central provides Reproducible Builds check status for projects published to Maven Central.

Example: org.apache.maven

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Applying filters to GitHub Tag and Release badges | Shields.io

**URL:** https://shields.io/blog/tag-filter

**Contents:**
- Applying filters to GitHub Tag and Release badges

We recently shipped a feature which allows you to pass an arbitrary filter to the GitHub tag and release badges. The filter param can be used to apply a filter to the project's tag or release names before selecting the latest from the list. Two constructs are available: * is a wildcard matching zero or more characters, and if the pattern starts with a !, the whole pattern is negated.

To give an example of how this might be useful, we create two types of tags on our GitHub repo: https://github.com/badges/shields/tags There are tags in the format major.minor.patch which correspond to our NPM package releases and tags in the format server-YYYY-MM-DD that correspond to our docker snapshot releases.

In our case, this would allow us to make a badge that applies the filter !server-* to filter out the snapshot tags and just select the latest package tag.

---

## GitHub commits since tagged version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-commits-since-tagged-version-branch

**Contents:**
- GitHub commits since tagged version (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: SubtitleEdit

Example: subtitleedit

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Terraform Module Downloads | Shields.io

**URL:** https://shields.io/badges/terraform-module-downloads

**Contents:**
- Terraform Module Downloads

Terraform Registry is an interactive resource for discovering a wide selection of integrations (providers), configuration packages (modules), and security rules (policies) for use with Terraform.

Possible values: [dw, dm, dy, dt]

Weekly, Monthly, Yearly or Total downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ore Stars | Shields.io

**URL:** https://shields.io/badges/ore-stars

**Contents:**
- Ore Stars

Ore is a Minecraft package repository.

Your Plugin ID is the name of your plugin in lowercase, without any spaces or dashes.

Example: https://ore.spongepowered.org/Erigitic/Total-Economy - Here the Plugin ID is totaleconomy.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io User Total Downloads | Shields.io

**URL:** https://shields.io/badges/crates-io-user-total-downloads

**Contents:**
- Crates.io User Total Downloads

Crates.io is a package registry for Rust.

The user ID can be found using https://crates.io/api/v1/users/{username}

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub go.mod Go version (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-go-mod-go-version-branch

**Contents:**
- GitHub go.mod Go version (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

The filename param can be used to specify the path to go.mod. By default, we look for go.mod in the repo root

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer quality (GitHub/Bitbucket) | Shields.io

**URL:** https://shields.io/badges/scrutinizer-quality-git-hub-bitbucket

**Contents:**
- Scrutinizer quality (GitHub/Bitbucket)

Scrutinizer quality (GitHub/Bitbucket)

Possible values: [g, b]

Platform: Either GitHub or Bitbucket

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Coverity Scan | Shields.io

**URL:** https://shields.io/badges/coverity-scan

**Contents:**
- Coverity Scan

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## DUB Downloads (specific version) | Shields.io

**URL:** https://shields.io/badges/dub-downloads-specific-version

**Contents:**
- DUB Downloads (specific version)

DUB Downloads (specific version)

Possible values: [dd, dw, dm, dt]

Daily, Weekly, Monthly, or Total downloads

This can either be a numeric version like 0.8.4 or the string latest

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Deps.rs Crate Dependencies (latest) | Shields.io

**URL:** https://shields.io/badges/deps-rs-crate-dependencies-latest

**Contents:**
- Deps.rs Crate Dependencies (latest)

Deps.rs is a service that checks dependency security status in Rust projects.

The name of the Rust crate

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## MyGet Downloads | Shields.io

**URL:** https://shields.io/badges/my-get-downloads

**Contents:**
- MyGet Downloads

Example: MongoDB.Driver.Core

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Factorio Mod Portal downloads | Shields.io

**URL:** https://shields.io/badges/factorio-mod-portal-downloads

**Contents:**
- Factorio Mod Portal downloads

Factorio Mod Portal downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Azure DevOps coverage | Shields.io

**URL:** https://shields.io/badges/azure-dev-ops-coverage

**Contents:**
- Azure DevOps coverage

Azure Devops (formerly VSO, VSTS) is Microsoft Azure's CI/CD platform.

To obtain your own badge, you need to get 3 pieces of information: ORGANIZATION, PROJECT_ID and DEFINITION_ID.

First, you need to select your build definition and look at the url:

Your badge will then have the form: https://img.shields.io/azure-devops/coverage/ORGANIZATION/PROJECT/DEFINITION_ID.svg.

Optionally, you can specify a named branch: https://img.shields.io/azure-devops/coverage/ORGANIZATION/PROJECT/DEFINITION_ID/NAMED_BRANCH.svg.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SourceForge Platform | Shields.io

**URL:** https://shields.io/badges/source-forge-platform

**Contents:**
- SourceForge Platform

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub code search count | Shields.io

**URL:** https://shields.io/badges/git-hub-code-search-count

**Contents:**
- GitHub code search count

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

For a full list of available filters and allowed values, see GitHub's documentation on Searching code

Example: goto language:javascript NOT is:fork NOT is:archived

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Pub Points | Shields.io

**URL:** https://shields.io/badges/pub-points

**Contents:**
- Pub Points

Pub is a package registry for Dart and Flutter.

This badge shows a measure of quality. This includes several dimensions of quality such as code style, platform support, and maintainability.

Example: analysis_options

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Dependents (via libraries.io) | Shields.io

**URL:** https://shields.io/badges/dependents-via-libraries-io

**Contents:**
- Dependents (via libraries.io)

Dependents (via libraries.io)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Pipenv locked Python version | Shields.io

**URL:** https://shields.io/badges/git-hub-pipenv-locked-python-version

**Contents:**
- GitHub Pipenv locked Python version

Pipenv is a dependency manager for Python which manages a virtualenv for projects. It adds/removes packages from your Pipfile as you install/uninstall packages and generates the ever-important Pipfile.lock, which can be checked in to source control in order to produce deterministic builds.

The GitHub Pipenv badges are intended for applications using Pipenv which are hosted on GitHub.

When Pipfile.lock is checked in, the GitHub Pipenv locked dependency version badge displays the locked version of a dependency listed in [packages] or [dev-packages] (or any of their transitive dependencies).

Usually a Python version is specified in the Pipfile, which pipenv lock then places in Pipfile.lock. The GitHub Pipenv Python version badge displays that version.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: rq-dashboard-on-heroku

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Jenkins Plugin Version | Shields.io

**URL:** https://shields.io/badges/jenkins-plugin-version

**Contents:**
- Jenkins Plugin Version

Jenkins Plugin Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## MyGet Downloads (tenant) | Shields.io

**URL:** https://shields.io/badges/my-get-downloads-tenant

**Contents:**
- MyGet Downloads (tenant)

MyGet Downloads (tenant)

MyGet Tenant in the format name.myget

Example: vs-devcore.myget

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitea Pull Requests | Shields.io

**URL:** https://shields.io/badges/gitea-pull-requests

**Contents:**
- Gitea Pull Requests

By default this badge looks for repositories on gitea.com. To specify another instance like codeberg, forgejo or a self-hosted instance, use the gitea_url query param.

Possible values: [all, all-raw, open, open-raw, closed, closed-raw]

Example: https://gitea.com

If you want to use multiple labels, you can use a comma (,) to separate them, e.g. foo,bar

Example: test,failure::new

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub package.json prod dependency version | Shields.io

**URL:** https://shields.io/badges/git-hub-package-json-prod-dependency-version

**Contents:**
- GitHub package.json prod dependency version

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: badge-maker/package.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Stack Exchange reputation | Shields.io

**URL:** https://shields.io/badges/stack-exchange-reputation

**Contents:**
- Stack Exchange reputation

Stack Exchange reputation

Example: stackoverflow

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Debian package (for distribution) | Shields.io

**URL:** https://shields.io/badges/debian-package-for-distribution

**Contents:**
- Debian package (for distribution)

Debian package (for distribution)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Steam Update Date | Shields.io

**URL:** https://shields.io/badges/steam-update-date

**Contents:**
- Steam Update Date

Using a web browser, you can find the ID in the url here:

In the steam client you can simply just Right-Click and 'Copy Page URL' and follow the above step

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bower License | Shields.io

**URL:** https://shields.io/badges/bower-license

**Contents:**
- Bower License

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## npms.io (scoped package) | Shields.io

**URL:** https://shields.io/badges/npms-io-scoped-package

**Contents:**
- npms.io (scoped package)

npms.io holds statistics for javascript packages.

Possible values: [final-score, maintenance-score, popularity-score, quality-score]

Example: maintenance-score

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Feedz Version | Shields.io

**URL:** https://shields.io/badges/feedz-version

**Contents:**
- Feedz Version

Possible values: [v, vpre]

version or version including pre-releases

Example: shieldstests

Example: MongoDB.Driver.Core

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Puppet Forge - PDK version | Shields.io

**URL:** https://shields.io/badges/puppet-forge-pdk-version

**Contents:**
- Puppet Forge - PDK version

Puppet Forge - PDK version

Example: azure_key_vault

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SourceForge Downloads (folder) | Shields.io

**URL:** https://shields.io/badges/source-forge-downloads-folder

**Contents:**
- SourceForge Downloads (folder)

SourceForge Downloads (folder)

Possible values: [dd, dw, dm, dt]

Daily, Weekly, Monthly, or Total downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Thunderstore Version | Shields.io

**URL:** https://shields.io/badges/thunderstore-version

**Contents:**
- Thunderstore Version

The Thunderstore badges require a package's namespace and name.

Everything can be discerned from your package's URL. Thunderstore package URLs have a mostly consistent format:

For example: https://thunderstore.io/c/lethal-company/p/notnotnotswipez/MoreCompany/.

The 'default community', Risk of Rain 2, has an alternative URL:

Some communities use a 'subdomain' alternative URL, for example, Valheim:

Example: notnotnotswipez

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Libraries.io SourceRank, scoped npm package | Shields.io

**URL:** https://shields.io/badges/libraries-io-source-rank-scoped-npm-package

**Contents:**
- Libraries.io SourceRank, scoped npm package

Libraries.io SourceRank, scoped npm package

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NuGet Downloads | Shields.io

**URL:** https://shields.io/badges/nu-get-downloads

**Contents:**
- NuGet Downloads

Example: Microsoft.AspNet.Mvc

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Factorio Mod Portal last updated | Shields.io

**URL:** https://shields.io/badges/factorio-mod-portal-last-updated

**Contents:**
- Factorio Mod Portal last updated

Factorio Mod Portal last updated

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io License (version) | Shields.io

**URL:** https://shields.io/badges/crates-io-license-version

**Contents:**
- Crates.io License (version)

Crates.io is a package registry for Rust.

Example: rustc-serialize

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gem download rank | Shields.io

**URL:** https://shields.io/badges/gem-download-rank

**Contents:**
- Gem download rank

Ruby Gems is a registry for ruby libraries

Possible values: [rt, rd]

total or daily ranking

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## TeamCity Coverage | Shields.io

**URL:** https://shields.io/badges/team-city-coverage

**Contents:**
- TeamCity Coverage

Example: FileHelpersStable

Example: https://teamcity.jetbrains.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Reddit User Karma | Shields.io

**URL:** https://shields.io/badges/reddit-user-karma

**Contents:**
- Reddit User Karma

Possible values: [link, comment, combined]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Open VSX Version | Shields.io

**URL:** https://shields.io/badges/open-vsx-version

**Contents:**
- Open VSX Version

Open VSX (OVSX) is a registry of extensions for VS Code compatible editors.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SourceForge Translations | Shields.io

**URL:** https://shields.io/badges/source-forge-translations

**Contents:**
- SourceForge Translations

SourceForge Translations

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub manifest version | Shields.io

**URL:** https://shields.io/badges/git-hub-manifest-version

**Contents:**
- GitHub manifest version

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: sindresorhus

Example: show-all-github-issues

Example: extension/manifest.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Greasy Fork Version | Shields.io

**URL:** https://shields.io/badges/greasy-fork-version

**Contents:**
- Greasy Fork Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub commit activity (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-commit-activity-branch

**Contents:**
- GitHub commit activity (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [w, m, y, t]

Commits in the last Week, Month, Year, or Total

Example: calebcartwright

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SymfonyInsight Grade | Shields.io

**URL:** https://shields.io/badges/symfony-insight-grade

**Contents:**
- SymfonyInsight Grade

SymfonyInsight (formerly SensioLabs) is a code analysis service

Example: 825be328-29f8-44f7-a750-f82818ae9111

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Vaadin Directory Version | Shields.io

**URL:** https://shields.io/badges/vaadin-directory-version

**Contents:**
- Vaadin Directory Version

Vaadin Directory Version

Example: vaadinvaadin-grid

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Discourse Likes | Shields.io

**URL:** https://shields.io/badges/discourse-likes

**Contents:**
- Discourse Likes

Example: https://meta.discourse.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Pub Version | Shields.io

**URL:** https://shields.io/badges/pub-version

**Contents:**
- Pub Version

Pub is a package registry for Dart and Flutter.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitea Forks | Shields.io

**URL:** https://shields.io/badges/gitea-forks

**Contents:**
- Gitea Forks

By default this badge looks for repositories on gitea.com. To specify another instance like codeberg, forgejo or a self-hosted instance, use the gitea_url query param.

Example: https://gitea.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bluesky posts | Shields.io

**URL:** https://shields.io/badges/bluesky-posts

**Contents:**
- Bluesky posts

Displays the number of Bluesky posts for a given handle using the public Bluesky API.

Bluesky handle (user ID)

Example: chitvs.bsky.social

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Gitea Stars | Shields.io

**URL:** https://shields.io/badges/gitea-stars

**Contents:**
- Gitea Stars

By default this badge looks for repositories on gitea.com. To specify another instance like codeberg, forgejo or a self-hosted instance, use the gitea_url query param.

Example: https://gitea.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM Type Definitions | Shields.io

**URL:** https://shields.io/badges/npm-type-definitions

**Contents:**
- NPM Type Definitions

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## DUB Downloads | Shields.io

**URL:** https://shields.io/badges/dub-downloads

**Contents:**
- DUB Downloads

Possible values: [dd, dw, dm, dt]

Daily, Weekly, Monthly, or Total downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Puppet Forge quality score | Shields.io

**URL:** https://shields.io/badges/puppet-forge-quality-score

**Contents:**
- Puppet Forge quality score

Puppet Forge quality score

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Azure DevOps releases | Shields.io

**URL:** https://shields.io/badges/azure-dev-ops-releases

**Contents:**
- Azure DevOps releases

To obtain your own badge, you need to get 4 pieces of information: ORGANIZATION, PROJECT_ID, DEFINITION_ID and ENVIRONMENT_ID.

First, you need to enable badges for each required environments in the options of your release definition. Once you have save the change, look at badge url:

Your badge will then have the form: https://img.shields.io/vso/release/ORGANIZATION/PROJECT_ID/DEFINITION_ID/ENVIRONMENT_ID.svg.

Example: 8cf3ec0e-d0c2-4fcd-8206-ad204f254a96

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PyPI - Versions from Framework Classifiers | Shields.io

**URL:** https://shields.io/badges/py-pi-versions-from-framework-classifiers

**Contents:**
- PyPI - Versions from Framework Classifiers

This service currently support the following Frameworks: AWS CDK, Django, Django CMS, JupyterLab, Odoo, Plone, Wagtail, Zope

Possible values: [aws-cdk, django, django-cms, jupyterlab, odoo, plone, wagtail, zope]

Example: https://pypi.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## bStats Servers | Shields.io

**URL:** https://shields.io/badges/b-stats-servers

**Contents:**
- bStats Servers

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub repo size | Shields.io

**URL:** https://shields.io/badges/git-hub-repo-size

**Contents:**
- GitHub repo size

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ansible Collection Downloads | Shields.io

**URL:** https://shields.io/badges/ansible-collection-downloads

**Contents:**
- Ansible Collection Downloads

Ansible Collection Downloads

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AppVeyor Build | Shields.io

**URL:** https://shields.io/badges/app-veyor-build

**Contents:**
- AppVeyor Build

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PyPI - Format | Shields.io

**URL:** https://shields.io/badges/py-pi-format

**Contents:**
- PyPI - Format

Example: https://pypi.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Issues or Pull Requests | Shields.io

**URL:** https://shields.io/badges/git-hub-issues-or-pull-requests

**Contents:**
- GitHub Issues or Pull Requests

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [issues, issues-raw, issues-closed, issues-closed-raw, issues-pr, issues-pr-raw, issues-pr-closed, issues-pr-closed-raw]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Steam File Size | Shields.io

**URL:** https://shields.io/badges/steam-file-size

**Contents:**
- Steam File Size

Using a web browser, you can find the ID in the url here:

In the steam client you can simply just Right-Click and 'Copy Page URL' and follow the above step

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ecologi (Trees) | Shields.io

**URL:** https://shields.io/badges/ecologi-trees

**Contents:**
- Ecologi (Trees)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bitbucket open issues | Shields.io

**URL:** https://shields.io/badges/bitbucket-open-issues

**Contents:**
- Bitbucket open issues

Bitbucket open issues

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab go.mod Go version | Shields.io

**URL:** https://shields.io/badges/git-lab-go-mod-go-version

**Contents:**
- GitLab go.mod Go version

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Example: gitlab-runner

The filename param can be used to specify the path to go.mod. By default, we look for go.mod in the repo root

Example: https://gitlab.example.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ansible Collection Version | Shields.io

**URL:** https://shields.io/badges/ansible-collection-version

**Contents:**
- Ansible Collection Version

Ansible Collection Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## YouTube Video Likes | Shields.io

**URL:** https://shields.io/badges/you-tube-video-likes

**Contents:**
- YouTube Video Likes

Shields.io is committed to protecting the privacy of its users. We do not collect, store, or track any personal information or data returned by the YouTube API Services. Our service is designed to generate badges based on public YouTube data, and we do not retain any user-specific information:

By using the YouTube badge, you are:

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Libraries.io dependency status for specific release | Shields.io

**URL:** https://shields.io/badges/libraries-io-dependency-status-for-specific-release

**Contents:**
- Libraries.io dependency status for specific release

Libraries.io dependency status for specific release

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Jenkins Plugin installs | Shields.io

**URL:** https://shields.io/badges/jenkins-plugin-installs

**Contents:**
- Jenkins Plugin installs

Jenkins Plugin installs

Example: view-job-filters

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Ubuntu Package Version (for series) | Shields.io

**URL:** https://shields.io/badges/ubuntu-package-version-for-series

**Contents:**
- Ubuntu Package Version (for series)

Ubuntu Package Version (for series)

Example: ubuntu-wallpapers

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WhatPulse | Shields.io

**URL:** https://shields.io/badges/what-pulse

**Contents:**
- WhatPulse

Possible values: [keys, clicks, uptime, download, upload]

Possible values: [user, team]

Either a user ID (e.g: 179734) or a group ID (e.g: dutch power cows)

show rank instead of value

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Tech Debt (branch) | Shields.io

**URL:** https://shields.io/badges/sonar-tech-debt-branch

**Contents:**
- Sonar Tech Debt (branch)

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: brave_brave-core

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Open Collective members by tier | Shields.io

**URL:** https://shields.io/badges/open-collective-members-by-tier

**Contents:**
- Open Collective members by tier
  - How to get the tierId

According to open collectives documentation, you can find the tierId by looking at the URL after clicking on a Tier Card on the collective page. (e.g. tierId for https://opencollective.com/shields/order/2988 is 2988)

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonar Fortify Security Rating | Shields.io

**URL:** https://shields.io/badges/sonar-fortify-security-rating

**Contents:**
- Sonar Fortify Security Rating

Note that the Fortify Security Rating badge will only work on Sonar instances that have the Fortify SonarQube Plugin installed. The badge is not available for projects analyzed on SonarCloud.io

The Sonar badges will work with both SonarCloud.io and self-hosted SonarQube instances. Just enter the correct protocol and path for your target Sonar deployment.

If you are targeting a legacy SonarQube instance that is version 5.3 or earlier, then be sure to include the version query parameter with the value of your SonarQube version.

Example: michelin_kstreamplify

Example: https://sonarcloud.io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Spack | Shields.io

**URL:** https://shields.io/badges/spack

**Contents:**
- Spack

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Discourse Posts | Shields.io

**URL:** https://shields.io/badges/discourse-posts

**Contents:**
- Discourse Posts

Example: https://meta.discourse.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bugzilla bug status | Shields.io

**URL:** https://shields.io/badges/bugzilla-bug-status

**Contents:**
- Bugzilla bug status

Use the baseUrl query parameter to target different Bugzilla deployments. If your Bugzilla badge errors, it might be because you are trying to load a private bug.

When not specified, this will default to https://bugzilla.mozilla.org.

Example: https://gcc.gnu.org/bugzilla

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io Version | Shields.io

**URL:** https://shields.io/badges/crates-io-version

**Contents:**
- Crates.io Version

Crates.io is a package registry for Rust.

Example: rustc-serialize

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Greasy Fork Rating | Shields.io

**URL:** https://shields.io/badges/greasy-fork-rating

**Contents:**
- Greasy Fork Rating

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CodeFactor Grade | Shields.io

**URL:** https://shields.io/badges/code-factor-grade

**Contents:**
- CodeFactor Grade

Possible values: [github, bitbucket]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer coverage (GitHub/Bitbucket) with branch | Shields.io

**URL:** https://shields.io/badges/scrutinizer-coverage-git-hub-bitbucket-with-branch

**Contents:**
- Scrutinizer coverage (GitHub/Bitbucket) with branch

Scrutinizer coverage (GitHub/Bitbucket) with branch

Possible values: [g, b]

Platform: Either GitHub or Bitbucket

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## SourceForge Last Commit | Shields.io

**URL:** https://shields.io/badges/source-forge-last-commit

**Contents:**
- SourceForge Last Commit

SourceForge Last Commit

The repository name, usually git but might be different.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CurseForge Version | Shields.io

**URL:** https://shields.io/badges/curse-forge-version

**Contents:**
- CurseForge Version

The CurseForge badge requires the Project ID in order access the CurseForge API.

The Project ID is different from the URL slug and can be found in the 'About Project' section of your CurseForge mod page.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Downloads (specific asset, latest release) | Shields.io

**URL:** https://shields.io/badges/git-hub-downloads-specific-asset-latest-release

**Contents:**
- GitHub Downloads (specific asset, latest release)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [downloads, downloads-pre]

downloads including or excluding pre-releases

Example: atom-amd64.deb

Whether to display the asset name in the badge value

Possible values: [date, semver]

Method used to determine latest release. Default: date

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Bitbucket open pull requests | Shields.io

**URL:** https://shields.io/badges/bitbucket-open-pull-requests

**Contents:**
- Bitbucket open pull requests

Bitbucket open pull requests

When not specified, this will default to https://bitbucket.org.

Example: https://bitbucket.mydomain.net

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Open VSX Downloads | Shields.io

**URL:** https://shields.io/badges/open-vsx-downloads

**Contents:**
- Open VSX Downloads

Open VSX (OVSX) is a registry of extensions for VS Code compatible editors.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## PyPI - Status | Shields.io

**URL:** https://shields.io/badges/py-pi-status

**Contents:**
- PyPI - Status

Example: https://pypi.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## UptimeObserver status | Shields.io

**URL:** https://shields.io/badges/uptime-observer-status

**Contents:**
- UptimeObserver status

UptimeObserver status

Example: 33Zw1rnH6veb4OLcskqvj6g9Lj4tnyxZ41

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub tag check runs | Shields.io

**URL:** https://shields.io/badges/git-hub-tag-check-runs

**Contents:**
- GitHub tag check runs

The Check Runs service shows the status of GitHub action runs.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Sonatype Nexus (Releases) | Shields.io

**URL:** https://shields.io/badges/sonatype-nexus-releases

**Contents:**
- Sonatype Nexus (Releases)

Sonatype Nexus (Releases)

Example: com.google.guava

Example: https://repo.tomkeuper.com

Note that you can use query options with any Nexus badge type (Releases, Snapshots, or Repository).

Query options should be provided as key=value pairs separated by a colon.

Possible values: Searching for Component

Example: :c=agent-apple-osx:p=tar.gz

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Dependents (via libraries.io), scoped npm package | Shields.io

**URL:** https://shields.io/badges/dependents-via-libraries-io-scoped-npm-package

**Contents:**
- Dependents (via libraries.io), scoped npm package

Dependents (via libraries.io), scoped npm package

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Vaadin Directory Rating Count | Shields.io

**URL:** https://shields.io/badges/vaadin-directory-rating-count

**Contents:**
- Vaadin Directory Rating Count

Vaadin Directory Rating Count

Example: vaadinvaadin-grid

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Uptime Robot ratio (30 days) | Shields.io

**URL:** https://shields.io/badges/uptime-robot-ratio-30-days

**Contents:**
- Uptime Robot ratio (30 days)

Uptime Robot ratio (30 days)

Example: m778918918-3e92c097147760ee39d02d36

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Voxel Shop Rating | Shields.io

**URL:** https://shields.io/badges/voxel-shop-rating

**Contents:**
- Voxel Shop Rating

You can find your resource ID in the url for your product page.

Example: https://voxel.shop/product/323/polymart-plugin - Here the Resource ID is 323.

Possible values: [rating, stars]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub deployments | Shields.io

**URL:** https://shields.io/badges/git-hub-deployments

**Contents:**
- GitHub deployments

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: shields-staging

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CTAN Version | Shields.io

**URL:** https://shields.io/badges/ctan-version

**Contents:**
- CTAN Version

CTAN is a package registry for TeX.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## npm package minimized gzipped size | Shields.io

**URL:** https://shields.io/badges/npm-package-minimized-gzipped-size

**Contents:**
- npm package minimized gzipped size

View esbuild minified and denoflate gzipped size of a javascript package or selected exports, via bundlejs.

This can either be a package name e.g: value-enhancer, or a package name and version e.g: value-enhancer@3.1.2

Example: value-enhancer@3.1.2

Example: lodash,axios

Possible values: [min, minzip, both]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Chocolatey Version | Shields.io

**URL:** https://shields.io/badges/chocolatey-version

**Contents:**
- Chocolatey Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WinGet Package Version | Shields.io

**URL:** https://shields.io/badges/win-get-package-version

**Contents:**
- WinGet Package Version

WinGet Community Repository

Example: Microsoft.WSL

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Org's stars | Shields.io

**URL:** https://shields.io/badges/git-hub-orgs-stars

**Contents:**
- GitHub Org's stars

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Note: This badge takes into account up to 200 of the most starred repositories of given user / org.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Static Badge | Shields.io

**URL:** https://shields.io/badges/static-badge

**Contents:**
- Static Badge

The static badge accepts a single required path parameter which encodes either:

Label, message and color separated by a dash -. For example: - https://img.shields.io/badge/any_text-you_like-blue

Message and color only, separated by a dash -. For example: - https://img.shields.io/badge/just%20the%20message-8A2BE2

Examples of additional options:

Style: - https://img.shields.io/badge/build-passing-brightgreen?style=for-the-badge

Color (named): - https://img.shields.io/badge/coverage-95%25-orange

Logo: - https://img.shields.io/badge/github-repo-blue?logo=github

Label, (optional) message, and color. Separated by dashes

Example: build-passing-brightgreen

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Discord | Shields.io

**URL:** https://shields.io/badges/discord

**Contents:**
- Discord

The Discord badge requires the SERVER ID in order access the Discord JSON API.

The SERVER ID can be located in the url of the channel that the badge is accessing.

To use the Discord badge a Discord server admin must enable the widget setting on the server.

Example: 308323056592486420

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer build (GitHub/Bitbucket) with branch | Shields.io

**URL:** https://shields.io/badges/scrutinizer-build-git-hub-bitbucket-with-branch

**Contents:**
- Scrutinizer build (GitHub/Bitbucket) with branch

Scrutinizer build (GitHub/Bitbucket) with branch

Possible values: [g, b]

Platform: Either GitHub or Bitbucket

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitLab Issues | Shields.io

**URL:** https://shields.io/badges/git-lab-issues

**Contents:**
- GitLab Issues

You may use your GitLab Project Id (e.g. 278964) or your Project Path (e.g. gitlab-org/gitlab ). Note that only internet-accessible GitLab instances are supported, for example https://jihulab.com, https://gitlab.gnome.org, or https://gitlab.com.

Possible values: [all, all-raw, open, open-raw, closed, closed-raw]

Example: gitlab-org/gitlab

Example: https://gitlab.com

If you want to use multiple labels, you can use a comma (,) to separate them, e.g. foo,bar

Example: test,failure::new

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Factorio Mod Portal mod version | Shields.io

**URL:** https://shields.io/badges/factorio-mod-portal-mod-version

**Contents:**
- Factorio Mod Portal mod version

Factorio Mod Portal mod version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Plugin Rating | Shields.io

**URL:** https://shields.io/badges/word-press-plugin-rating

**Contents:**
- WordPress Plugin Rating

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Dynamic YAML Badge | Shields.io

**URL:** https://shields.io/badges/dynamic-yaml-badge

**Contents:**
- Dynamic YAML Badge

The Dynamic YAML Badge allows you to extract an arbitrary value from any YAML Document using a JSONPath selector and show it on a badge.

The URL to a YAML document

Example: https://raw.githubusercontent.com/badges/shields/master/.github/dependabot.yml

A JSONPath expression that will be used to query the document

Optional prefix to append to the value

Optional suffix to append to the value

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Steam Favorites | Shields.io

**URL:** https://shields.io/badges/steam-favorites

**Contents:**
- Steam Favorites

Using a web browser, you can find the ID in the url here:

In the steam client you can simply just Right-Click and 'Copy Page URL' and follow the above step

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## LuaRocks | Shields.io

**URL:** https://shields.io/badges/lua-rocks

**Contents:**
- LuaRocks

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## CII Best Practices | Shields.io

**URL:** https://shields.io/badges/cii-best-practices

**Contents:**
- CII Best Practices

The Core Infrastructure Initiative (CII) Best Practices badge is a way for Open Source projects to show that they follow best practices

Possible values: [level, percentage, summary]

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Discussions | Shields.io

**URL:** https://shields.io/badges/git-hub-discussions

**Contents:**
- GitHub Discussions

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Voxel Shop Downloads | Shields.io

**URL:** https://shields.io/badges/voxel-shop-downloads

**Contents:**
- Voxel Shop Downloads

You can find your resource ID in the url for your product page.

Example: https://voxel.shop/product/323/polymart-plugin - Here the Resource ID is 323.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub forks | Shields.io

**URL:** https://shields.io/badges/git-hub-forks

**Contents:**
- GitHub forks

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## JSR Version | Shields.io

**URL:** https://shields.io/badges/jsr-version

**Contents:**
- JSR Version

JSR is a modern package registry for JavaScript and TypeScript.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Arch Linux package | Shields.io

**URL:** https://shields.io/badges/arch-linux-package

**Contents:**
- Arch Linux package

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Modrinth Game Versions | Shields.io

**URL:** https://shields.io/badges/modrinth-game-versions

**Contents:**
- Modrinth Game Versions

You can use your project slug, or the project ID. The ID can be found in the 'Technical information' section of your Modrinth page.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub followers | Shields.io

**URL:** https://shields.io/badges/git-hub-followers

**Contents:**
- GitHub followers

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Azure DevOps tests | Shields.io

**URL:** https://shields.io/badges/azure-dev-ops-tests

**Contents:**
- Azure DevOps tests

Azure Devops (formerly VSO, VSTS) is Microsoft Azure's CI/CD platform.

To obtain your own badge, you need to get 3 pieces of information: ORGANIZATION, PROJECT, DEFINITION_ID.

First, you need to select your build definition and look at the url:

Your badge will then have the form: https://img.shields.io/azure-devops/tests/ORGANIZATION/PROJECT/DEFINITION_ID.svg.

Optionally, you can specify a named branch: https://img.shields.io/azure-devops/tests/ORGANIZATION/PROJECT/DEFINITION_ID/NAMED_BRANCH.svg.

You may change the "passed", "failed" and "skipped" text on this badge by supplying query parameters &passed_label=, &failed_label= and &skipped_label= respectively.

For example, if you want to use a different terminology:

?passed_label=good&failed_label=bad&skipped_label=n%2Fa

?compact_message&passed_label=💃&failed_label=🤦‍♀️&skipped_label=🤷

There is also a &compact_message query parameter, which will default to displaying ✔, ✘ and ➟, separated by a horizontal bar |.

Example: azuredevops-powershell

Example: azuredevops-powershell

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## NPM (prod) Dependency Version | Shields.io

**URL:** https://shields.io/badges/npm-prod-dependency-version

**Contents:**
- NPM (prod) Dependency Version

NPM (prod) Dependency Version

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: react-boxplot

This may be the name of an unscoped package like package-name or a scoped package like @author/package-name

Example: simple-statistics

Example: https://registry.npmjs.com

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Keybase PGP | Shields.io

**URL:** https://shields.io/badges/keybase-pgp

**Contents:**
- Keybase PGP

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "social".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## MyGet Version | Shields.io

**URL:** https://shields.io/badges/my-get-version

**Contents:**
- MyGet Version

Possible values: [v, vpre]

Latest stable version (v) or Latest version including prereleases (vpre).

Example: MongoDB.Driver.Core

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## WordPress Theme Active Installs | Shields.io

**URL:** https://shields.io/badges/word-press-theme-active-installs

**Contents:**
- WordPress Theme Active Installs

These badges rely on an API that is no longer supported by Wordpress. You are still free to use them, simply bear in mind that Shields.io cannot guarantee that they'll keep on working in the future. Please also double-check the provided slug, as an incorrect value may lead to unexpected results.

Example: twentyseventeen

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Crates.io Downloads (version) | Shields.io

**URL:** https://shields.io/badges/crates-io-downloads-version

**Contents:**
- Crates.io Downloads (version)

Crates.io is a package registry for Rust.

Example: rustc-serialize

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## AppVeyor tests (with branch) | Shields.io

**URL:** https://shields.io/badges/app-veyor-tests-with-branch

**Contents:**
- AppVeyor tests (with branch)

You may change the "passed", "failed" and "skipped" text on this badge by supplying query parameters &passed_label=, &failed_label= and &skipped_label= respectively.

For example, if you want to use a different terminology:

?passed_label=good&failed_label=bad&skipped_label=n%2Fa

?compact_message&passed_label=💃&failed_label=🤦‍♀️&skipped_label=🤷

There is also a &compact_message query parameter, which will default to displaying ✔, ✘ and ➟, separated by a horizontal bar |.

Example: coap-net-iu0to

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Scrutinizer build (GitLab) | Shields.io

**URL:** https://shields.io/badges/scrutinizer-build-git-lab

**Contents:**
- Scrutinizer build (GitLab)

Scrutinizer build (GitLab)

Example: propertywindow

Example: propertywindow

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub Pipenv locked dependency version | Shields.io

**URL:** https://shields.io/badges/git-hub-pipenv-locked-dependency-version

**Contents:**
- GitHub Pipenv locked dependency version

Pipenv is a dependency manager for Python which manages a virtualenv for projects. It adds/removes packages from your Pipfile as you install/uninstall packages and generates the ever-important Pipfile.lock, which can be checked in to source control in order to produce deterministic builds.

The GitHub Pipenv badges are intended for applications using Pipenv which are hosted on GitHub.

When Pipfile.lock is checked in, the GitHub Pipenv locked dependency version badge displays the locked version of a dependency listed in [packages] or [dev-packages] (or any of their transitive dependencies).

Usually a Python version is specified in the Pipfile, which pipenv lock then places in Pipfile.lock. The GitHub Pipenv Python version badge displays that version.

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: rq-dashboard-on-heroku

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Weblate project translated | Shields.io

**URL:** https://shields.io/badges/weblate-project-translated

**Contents:**
- Weblate project translated

Weblate is an web-based tool for translation and internationalization

Example: godot-engine

Example: https://hosted.weblate.org

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub package.json dynamic (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-package-json-dynamic-branch

**Contents:**
- GitHub package.json dynamic (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

any key in package.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub manifest.json dynamic (branch) | Shields.io

**URL:** https://shields.io/badges/git-hub-manifest-json-dynamic-branch

**Contents:**
- GitHub manifest.json dynamic (branch)

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Example: sindresorhus

Example: show-all-github-issues

Example: extension/manifest.json

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub commit activity | Shields.io

**URL:** https://shields.io/badges/git-hub-commit-activity

**Contents:**
- GitHub commit activity

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

Possible values: [w, m, y, t]

Commits in the last Week, Month, Year, or Total

Example: calebcartwright

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Cocoapods platforms | Shields.io

**URL:** https://shields.io/badges/cocoapods-platforms

**Contents:**
- Cocoapods platforms

Example: AFNetworking

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Puppet Forge modules by user | Shields.io

**URL:** https://shields.io/badges/puppet-forge-modules-by-user

**Contents:**
- Puppet Forge modules by user

Puppet Forge modules by user

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Homebrew Formula Version | Shields.io

**URL:** https://shields.io/badges/homebrew-formula-version

**Contents:**
- Homebrew Formula Version

Homebrew Formula Version

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Vaadin Directory Release Date | Shields.io

**URL:** https://shields.io/badges/vaadin-directory-release-date

**Contents:**
- Vaadin Directory Release Date

Vaadin Directory Release Date

Example: vaadinvaadin-grid

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## REUSE Compliance | Shields.io

**URL:** https://shields.io/badges/reuse-compliance

**Contents:**
- REUSE Compliance

Example: github.com/fsfe/reuse-tool

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## GitHub issue custom search in repo | Shields.io

**URL:** https://shields.io/badges/git-hub-issue-custom-search-in-repo

**Contents:**
- GitHub issue custom search in repo

You can help increase Shields.io's rate limit by authorizing the Shields.io GitHub application. Read more about how it works.

For a full list of available filters and allowed values, see GitHub's documentation on Searching issues and pull requests

Example: is:closed label:bug author:app/sentry-io

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---

## Localizely progress | Shields.io

**URL:** https://shields.io/badges/localizely-progress

**Contents:**
- Localizely progress

Localizely is a management system for translation, localization, and internationalization of your projects.

The read-only API token from the Localizely account is required to fetch necessary data.

Note: Do not use the default API token as it grants full read-write permissions to your projects. You will expose your project and allow malicious users to modify the translations at will. Instead, create a new one with only read permission.

You can find more details regarding API tokens under My profile page.

Example: 5cc34208-0418-40b1-8353-acc70c95f802

Example: 0f4d5e31a44f48dcbab966c52cfb0a67c5f1982186c14b85ab389a031dbc225a

Possible values: [flat, flat-square, plastic, for-the-badge, social]

If not specified, the default style for this badge is "flat".

Icon slug from simple-icons. You can click the icon title on simple-icons to copy the slug or they can be found in the slugs.md file in the simple-icons repository. Further info.

The color of the logo (hex, rgb, rgba, hsl, hsla and css named colors supported). Supported for simple-icons logos but not for custom logos.

Make icons adaptively resize by setting auto. Useful for some wider logos like amd and amg. Supported for simple-icons logos but not for custom logos.

Override the default left-hand-side text (URL-Encoding needed for spaces or special characters!)

Background color of the left part (hex, rgb, rgba, hsl, hsla and css named colors supported).

Background color of the right part (hex, rgb, rgba, hsl, hsla and css named colors supported).

HTTP cache lifetime (rules are applied to infer a default value on a per-badge basis, any values specified below the default will be ignored).

Specify what clicking on the left/right of a badge should do. Note that this only works when integrating your badge in an <object> HTML tag, but not an <img> tag or a markup language.

---
