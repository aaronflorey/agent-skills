# gh extension

- Docs: `https://cli.github.com/manual/gh_extension`
- Use to install, run, upgrade, search, or remove `gh` extensions
- Key subcommands: `install`, `list`, `search`, `exec`, `upgrade`, `remove`, `create`
- Example:

```bash
gh extension search
gh extension install owner/gh-my-ext
gh extension list
```
