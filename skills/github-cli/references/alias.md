# gh alias

- Docs: `https://cli.github.com/manual/gh_alias`
- Use to create reusable shortcuts for long `gh` commands
- Key subcommands: `set`, `list`, `delete`, `import`
- Example:

```bash
gh alias set pv 'pr view'
gh alias list
gh pv 123
```
