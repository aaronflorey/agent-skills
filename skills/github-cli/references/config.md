# gh config

- Docs: `https://cli.github.com/manual/gh_config`
- Use to inspect or change `gh` configuration values
- Key subcommands: `list`, `get`, `set`, `clear-cache`
- Example:

```bash
gh config list
gh config get editor
gh config set git_protocol ssh
```
