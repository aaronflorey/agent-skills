# gh ssh-key

- Docs: `https://cli.github.com/manual/gh_ssh-key`
- Use to add, list, or delete SSH keys on GitHub
- Key subcommands: `add`, `list`, `delete`
- Example:

```bash
gh ssh-key list
gh ssh-key add ~/.ssh/id_ed25519.pub
```
