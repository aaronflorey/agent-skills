# Installation

Source page: https://docs.saloon.dev/the-basics/installation

- Install Saloon with Composer before creating connectors or requests.
- Start with one connector per upstream API.
- Keep Saloon as the transport and integration layer rather than mixing request code across unrelated services.
- After install, the usual first step is a connector plus a request class.
