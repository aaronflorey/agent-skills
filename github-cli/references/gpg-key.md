# gh gpg-key

- Docs: `https://cli.github.com/manual/gh_gpg-key`
- Use to add, list, or delete GPG keys on GitHub
- Key subcommands: `add`, `list`, `delete`
- Example:

```bash
gh gpg-key list
gh gpg-key add mykey.asc
```
