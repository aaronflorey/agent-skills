# gh completion

- Docs: `https://cli.github.com/manual/gh_completion`
- Use to generate shell completions for `bash`, `zsh`, `fish`, or `powershell`
- Example:

```bash
gh completion bash
gh completion zsh
```
