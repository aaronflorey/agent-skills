# gh preview

- Docs: `https://cli.github.com/manual/gh_preview`
- Use to run preview or experimental `gh` features
- Best practice: inspect available preview namespaces locally with `gh preview --help`
- Example:

```bash
gh preview --help
```
