# gh attestation

- Docs: `https://cli.github.com/manual/gh_attestation`
- Use to verify or download artifact attestations and trusted roots
- Key subcommands: `verify`, `download`, `trusted-root`
- Example:

```bash
gh attestation verify artifact.tar.gz
gh attestation trusted-root
```
