# gh ruleset

- Docs: `https://cli.github.com/manual/gh_ruleset`
- Use to inspect or evaluate repository rulesets
- Key subcommands: `check`, `list`, `view`
- Example:

```bash
gh ruleset list -R OWNER/REPO
gh ruleset check -R OWNER/REPO
```
