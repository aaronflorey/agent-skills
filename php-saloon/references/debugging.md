# Debugging

Source page: https://docs.saloon.dev/the-basics/debugging

- Use `debug()` to inspect both the request and response.
- Use `debugRequest()` or `debugResponse()` when you only need one side.
- Custom debug handlers are supported.
- The debug output reflects the request right before it is handed to the sender.
