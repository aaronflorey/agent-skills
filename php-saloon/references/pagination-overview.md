# Pagination Overview

Source page: https://docs.saloon.dev/installable-plugins/pagination

- Pagination is provided through an installable plugin.
- Supported styles include paged, limit/offset, cursor, and fully custom pagination.
- Paginators are iterators and keep only one page in memory at a time.
- If the upstream API supports pagination, SDK packages should expose it.
