pre-commit:
  jobs:
    - name: update README skill table
      run: bun scripts/update-readme-skills.js
      stage_fixed: true

commit-msg:
  jobs:
    - name: bump skill versions
      run: bunx --bun zx scripts/bump-skill-versions.mjs {1}
